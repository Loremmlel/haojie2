# 维护约定

## 不变量

生产构建必须只产生一个可离线运行的HTML，不允许CDN、字体服务、图片服务器、运行时动态分包或隐式网络请求。React组件公开入口为`src/index.ts`，无框架规则入口为`src/engine/index.ts`。

引擎禁止依赖React、DOM、本地存储、音频或系统时间。所有操作通过`applyCommand`执行，输入状态不可变；随机数必须来自状态中的PRNG。新技能不得在React事件处理器中直接修改生命、归属、行动次数或召唤结果。

棋子基础数据、名称与能力说明统一维护在`catalog.ts`。涉及规则歧义的更改必须同步游戏内解释、`docs/RULES.md`和行为测试，不要把未经确认的解释当作已确认原意。

`Point`参数可能由包含额外属性的对象传入，创建单位时只复制x/y，不要把旧单位的id或kind展开到新单位。动画事件保存坐标快照，不能引用随后会被修改的棋子对象。

## 验证

提交前执行格式检查、TypeScript检查、引擎测试、单HTML构建和离线浏览器验收。浏览器验收在GitHub Actions使用Chromium；某些受管理环境禁止`file://`，应明确区分本地预览与CI真实离线验收，不能把未运行的测试声称为通过。

测试优先验证每种棋子的稳定外部行为、结算时序与命令原子性，不为了内部重构持续堆叠同类回归测试。不把CSS类排列、React内部状态或整页像素截图作为核心规则测试依据。

## 页面与嵌入

样式以`.hj-game`作用域限定，避免污染宿主。动画不参与规则结算，并提供减少动态效果模式。关闭音效是默认行为。新增图标按钮应有可访问名称，不能仅依赖桌面可见但手机隐藏的文字。

`initialState`仅控制初始挂载；宿主替换对局使用新的React key。独立游戏为同屏双人，不要宣传未实现的联网、房间、AI或服务端防作弊。

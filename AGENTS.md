# 浩劫维护约定

## 来源和版本

本轮规则来源为`docs/source/浩劫.docx`，逐条差异见`docs/CHANGELOG-2.0.md`。来源优先级：作者回复 > 新增段落的明确修改 > 一般规则 > 旧版猜测。不要把“第四属性”再改回通用行动次数。作者后续确认冲锋号令储存8回合；善铁和相关合成为暂缓的3.0神龛模式，不提前加入2.x。

## 不变量

生产只生成一个离线HTML。禁止运行时CDN、图片/字体服务器、分包和隐式网络请求。引擎入口`src/engine/index.ts`不得依赖React、DOM、本地存储、音频、时钟或网络。UI入口`src/index.ts`提供React组件。

所有游戏变化通过`applyCommand`。输入状态不可变，非法命令不部分支付人头/卡牌/随机数。UI预览不消耗正式随机数。图鉴和属性统一维护在`catalog.ts`，不要另建一份会漂移的UI数值表。

全局时钟与单个随从时钟分开；实际部署时刻不能因冲锋号令而改变。完整操作、攻击次数、冲撞步数、武器额外攻击是不同预算，禁止混用。0攻击次数不等于不能移动。

叠放和2×2占位必须通过geometry函数处理。单体敌方目标取栈顶，区域法术取全栈；AOE敌方技能仍取栈顶，炎魔之王专属例外。克隆人头按group最后死亡，包含手中未部署同批卡。创建衍生物只复制坐标，不展开旧棋子导致id/kind/效果继承。

死亡选择、弹出、小屋召唤都进入可序列化反应队列；动画不能驱动伤害结算。事件只保存坐标快照。持续状态、来源归属、延迟区域和连接必须可悔棋、可存档。

## 验证

提交前运行格式、TypeScript、行为测试、构建和离线浏览器验收。行为测试按core/ultimate/session组织，尽量稳定维护每个能力的可观察行为；不按实现细节写脆弱断言，不用大量重复回归或像素快照约束重构。

CI默认直接打开离线file://生产HTML。沙箱可能限制导航；内存渲染模式可检查UI，但报告必须标明模式，不可声称验证了文件打开或本机持久化。不能绕过浏览器管理策略。

## 嵌入和文档

样式用`.hj-game`作用域，不污染宿主。按钮有可访问名称，手机隐藏文字后仍可操作；尊重减少动态效果、声音默认关闭。当前支持同屏双人和本地AI，手牌公开；不宣传未实现的联网或云AI功能。v1局面不自动迁移v2，不删除用户旧存档。

若更改未注明项的实施解释，同步RULES、游戏内说明、图鉴和对应行为测试，标明哪些已由作者确认，哪些仍待确认。

## 工程组织与发布

UI按game/board/inspector/hand/library/feedback/session/shared分类；Game.tsx只组合页面。组件就近维护CSS，基础token和最终减少动态效果规则在styles/。新增手机样式写入所属组件CSS，不恢复styles.css/v2.css补丁堆。维持原生CSS+SVG方案，不为行数引入组件库或动画库。

源码修改影响页面时，运行`npm run deploy`生成根index.html，与源码一同提交。Pages使用main/(root)，.nojekyll禁用Jekyll；CI使用deploy:check拒绝过期成品。不让GITHUB_TOKEN自动提交发布物后假设它会触发Pages。发布编号与存档schema版本分开；工程重构不得更换存档键或清空localStorage。

## AI不变量（2.1）

AI代码在src/ai，不把评分/搜索搬入React或引擎。只接收observe白名单，禁止传入真实seed/rng、整份Session或通过历史反推未来PRNG。规则随机变化必须为模拟源提供正确概率分界；不能把平均伤害当作必杀概率。规则命令的默认PRNG语义保持不变。

困难档是预算受限的束搜索和对手回应，不宣称全宽minimax或MCTS。预算中断不要比较不同回合阶段的分数。强制反应、未部署手牌和过长连锁必须合法完成或明确暂停，不得静默跳过。

所有任务都要在新局、载入、悔棋和卸载时取消；旧结果即使返回也不得落子。人机悔棋回到人类决策节点，反应所属方可与active不同，不能抢走人类反应。存档match缺失默认local，保存humanAnchor与已知迁移兼容。

宽屏（游戏容器宽度≥1100px）优先保持完整9×13正方形棋盘与结束回合/悔棋等控制一屏可用，页面和棋盘不滚动，只有侧栏长内容独立滚动。顶部紧凑，比分/AI状态/指令利用横向空间；不要重新施加“棋盘宽度必须≥620px”的验收条件，也不能用body overflow:hidden裁掉棋盘伪装一屏。棋盘从实际容器剩余宽高等比适配，宿主可用--haojie-height指定可用高度；手机保留自然文档流。响应式规则留在功能CSS。CI保留原100项核心契约，新增AI行为与发行HTML验收，不用组件私有状态和整页像素快照约束重构。

AI展示节奏与计算预算分离，保持在ui/opponent/pacing.ts。真实计算与展示间隔重叠；缓存连招不得绕过节奏。等待需可立即取消，并在实际执行前重新校验请求与局面。不能调用规则PRNG来随机延迟，不能把表现层停顿保存成游戏时间或用于改变棋力，不能为延迟破坏宽屏一屏布局。

AI实战反馈与CLI：保留作者原始实战文档，未决对局不按血量判胜。CLI必须共用引擎和公开Observation，回放记录真实随机状态仅用于重放，不能送给AI。危险预测要考虑下一实际行动窗口、就绪蓄力与路径；全局风险成本不能让无增益的受伤变成正收益，临时蓄力不要重复当永久火力。连射扩展遇随机/反应必须停；只在实际结果匹配时复用计划。性能、棋力、规则正确性分开验证，禁止把固定种子自对弈率称为对熟练作者胜率。

AI预算审计约定：浏览器与CLI默认work固定工作量，墙钟不能影响预算分配或选招；限时模式必须显式标记timed。保持同Observation/难度/有效预算的确定性，不把“同局面但不同预算历史”误称为同一实验。困难回应只采纳完整成对场景，后续场景中断不得丢弃已完成轮次，也不得混入半回合分数。begin不跨阶段缓存整回合。stats区分缓存、己方搜索链、选中计划、算完的回应和真正采用的回应；改指标语义要更新文档/审计脚本。攻击规则概率由catalog/attack-profile共享，AI策略估值继续留在AI层；维护原PRNG分界与消耗顺序并验证旧回放。

特效维护：遵循docs/VFX.md。引擎事件只补充事实、身份/尺寸快照和因果关系，不为视觉增加事件ID/消耗serial或规则PRNG。表现映射不判断中文文案、不复制规则数值；施加印记与触发成功、免疫/反制要分开。动画不能驱动结算，持续状态由当前局面决定。批次独立回收且有上限；替换会话/卸载取消原生动画，重做/载入不重播历史事件。保留实际路径与大体型中心，减少动态效果不能把飞行物留在原点。修改后运行特效浏览器验收与历史CLI指纹回放，开发预览不进入发行HTML。

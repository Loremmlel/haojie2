# 浩劫：既有界面设计契约

## 产品与基调

中文、可完全离线的同屏/本地AI战棋；棋盘优先。沿用既有纸面、淡墨、青绿/朱红阵营色与简洁几何特效，不为规则更新重设计整站。运行时颜色与字体由 `src/ui/styles/tokens.css` 管理；阵营语义由 `src/ui/shared/visuals.tsx` 的 `--side` / `--side-soft` 提供，本次不新增色板或字体。

## 布局与共用组件

桌面棋盘保持正方形格子，左右功能栏独立滚动；窄屏遵循 `src/ui/game/layout.css` 的现有流式布局。按钮和提示复用现有样式；弹窗复用 `shared/Modal`，不使用浏览器原生确认框。新规则选择不得新增页面高度固定值或改变宿主滚动。

## 本轮操作约定

冲锋怪先选“正常部署”或“扣10血 · 冲锋部署”，再点棋盘；选点期间展示真实生命代价，不再以隐藏勾选框改变已选模式。攻击路径先选目标，再从最多四个最后一步方向中选择；箭头方向就是伤害进入方向，数字是整条路径长度。SVG仅预览，不拦截指针、改变局面或驱动伤害；与实际命令共用引擎路径。选点格保留原生按钮、中文可访问名称、方向键与Enter，点击即可完成，不依赖悬停。取消不花费操作，悔棋回滚整个攻击及随机结果。

额外召唤复用手牌区 `SummonControls`，明确“本回合有效”，不混入下一回合奖励。数值与能力说明以 catalog 和 `docs/RULES.md` 为准，界面不独立实现规则。

## 棋子状态与改判入口（2026-09-17）

规则来源为 `docs/FEEDBACK-2026-09-17.md` 与 `docs/RULES.md`。棋子详情中的 `UnitStatus` 统一呈现“当前状态”，只读模型为 `inspector/unit-status.ts`；时钟、来源名刀和光环范围复用引擎查询，不另存一份UI状态。每行显示名称与解释，待生效必须有文字标识，名刀显示剩余次数及来源坐标/已消耗状态，装备显示实际能力说明。无额外状态时明确显示空态。延续现有紧凑侧栏，不在棋盘上堆长标签。

召唤完成后的改判由手牌区 `SummonControls` / `RerollControls` 承载，与手牌详情及所有AI难度共用 `engine/summoning.ts` 的合法选项。原生 `details/summary` 提供键盘和触摸均可用的“是，选择召唤结果”，展开/收起不花资源；保留“完成召唤，开始行动”作为不改判的出口。替换结果后显示剩余来源次数，可继续改判；不新增阻塞弹窗或强制确认。操作结果、错误、悔棋沿用现有会话流程，窄屏按钮换行而非横向溢出。

## 回合开始合成（2.5）

规则来源 `docs/CHANGELOG-2.5.md`。手牌区 `SynthesisControls` 复用既有按钮、原生fieldset/checkbox与棋盘选点，不新增弹窗、页面高度或字体。配方为单选按钮组，材料恰好3个，名称旁提供坐标/血量及装备会移除的提示；未选满或无合法落点时禁用确认并说明原因。材料列表使用同一侧栏滚动，不另加固定高度。第4个未选材料暂时禁选，可先取消已选项换人。

先选配方和材料，再选择棋盘落点，一次提交移除和召唤。取消不花资源，悔棋恢复整个合成。无配方/主动跳过进入原召唤流程，无隐藏自动交易。角色详情保留新光环来源、半速蓄力与被动状态。牵引在原反应操作栏明确“牵引命中目标 / 放弃此效果”，王城有合法位置时必须选点，否则允许关闭失效反应；都不依赖悬停。窄屏保持自然流式布局、键盘与触摸可操作。

## 验证

`tests/browser/game.mjs` 覆盖部署、方向选择、取消/悔棋、额外召唤、蓄力条、多来源改判/自我重铸、状态面板、390px/键盘交互；`tests/browser/ai.mjs` 覆盖AI及多宽度布局。真实file://离线验收由CI执行；沙箱setContent结果只称渲染与交互验证。

## 神龛工作流（3.0）

业务来源 `docs/CHANGELOG-3.0.md` 与 `docs/RULES.md`。保留既有token/字体/格线与侧栏滚动所有权，不新建视觉主题。开局沿用 `MatchOptions` 原生radio区分规则模式和对战模式，不把两者揉成一个枚举。`ShrineControls` 在手牌区显示本人3张候选与可展开的对方候选；共同揭示前不读对方私有结果，不因AI正在选择而换成人类看不见的AI私有页面。

地标用格内底座+名称/血量（或休眠计数）表示，与随从图形分层；叠放详情的按钮和连续点击棋盘使用同一候选次序。需要选单个目标时，`ActionPanel` 提供“棋子优先 / 选地标”，仍以引擎合法高亮为准，非法点保持操作而不花费资源。地标休眠详情无无效行动按钮；`UnitStatus` 显示倒计时和占位等待原因。

永久光环由手牌侧栏 `AuraPanel` 展示双方状态；时钟沿用棋盘目标选择、取消、错误提示和悔棋。`SummonControls` 统一承载免费常驻与付人头额外召唤；`OfferChoice` 使用原生checkbox恰选2个，克隆一整批单列。牢千K沿用 `SynthesisControls` 的三材料确认，但光环不需落点；原生select只负责下一次匹配来源池的自选计划，不提前消费次数。

规范所有者：规则数值catalog；操作合法性engine；选择/取消/反馈useGameController与现有Toast；模态shared/Modal；棋盘层选择Inspector/ActionPanel；原生radio/checkbox/select与details接受平台键盘/弹层行为。自选计划只在本地UI暂存，换局/载入/悔棋清除。成功/错误不移动棋盘，手机侧栏继续文档流。`tests/browser/shrine.mjs`覆盖全部新流、双方人机身份、Worker/回退、390px、键盘与零外部网络请求。

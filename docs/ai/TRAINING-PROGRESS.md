# AI训练进度

本文件持续记录已完成工作、当前实验、复现入口和下一步。最后更新：2026-09-22。训练路线见[TRAINING.md](TRAINING.md)，Python用法见[training/README.md](../../training/README.md)。只有实际运行并通过验收的项目才标记完成。

## 目标与约束

- 首个交付目标：真实公开局面编码 → 教师样本 → 策略/价值训练 → 合法完整对局 → 离线浏览器推理。
- 继续接入含规则随机事件的MCTS与分轮自对弈；对作者超过80%胜率保留为独立实战目标，不用训练loss、固定种子自对弈或短局代替。
- 先经典模式打通；神龛状态须能编码，暗选搜索另做信息边界验收。双方手牌公开，正式seed/rng不进入网络或搜索。
- 引擎、几何、能力与合法性查询继续复用TypeScript；训练在仓库内独立Python环境进行。模型、数据、临时报告放artifacts/training，源码与小型实验摘要入Git。
- 当前约11.70M网络，XPU BF16主干＋FP32候选评分、batch 32；CPU FP32作为参照。原11.55M/64实体合成基准中的batch 128不是当前真实数据的固定配置。

## 阶段状态

| 阶段                            | 状态     | 验收与下一步                                                                              |
| ------------------------------- | -------- | ----------------------------------------------------------------------------------------- |
| M0 高速环境、模型骨架和设备基准 | 完成     | 常驻Node环境、教师JSONL、11.55M骨架、保存续训、18组CPU/XPU精度基准                        |
| M1 真实状态与动作编码           | 首版完成 | 79条正式回放逐条结算一致；路径/材料/反应/暗选/大局面测试；3404个教师命令全部转换，无丢样  |
| M2 少量真实数据训练与部署可行性 | 进行中   | 64样本拟合、ONNX导出、浏览器本地HTTP前向、20局完整CLI网络对战通过；离线打包与Worker待完成 |
| M3 第一轮教师预训练             | 待完成   | 先几十局检查终局与能力覆盖；再视结果扩至5万–20万决策样本；独立验证集与对抗评测            |
| M4 小预算MCTS                   | 待完成   | Node搜索、批量网络评估、随机边界及决策所属方正确；32/64再到128/256模拟对照                |
| M5 分轮自对弈与最终实战         | 待完成   | 历史对手、败局/罕见能力补样、固定预算的开发评测；最终作者测试独立保留                     |

M1设计：主棋子、手牌、基地及其附属状态分别为实体记录；来源和身份映射到局内引用，不解析原始ID中的序列号。模型使用分步动作选择，路径逐格延伸、材料逐个选择；公开参数域与完整合法命令分开标识，不能把教师裁剪候选宣称为完整动作空间。

具体特征、标签和解码边界见[编码说明](../../training/ENCODING.md)，可复现命令见[Python用法](../../training/README.md)。M1首版通过当前回放和专项局面，不能推断已覆盖所有罕见能力组合。

## 当前实验：真实样本流程检查

目录：`artifacts/training/pilot-20260922/`。经典模式、easy教师、每决策40工作量节点、种子2026092201–2026092204，每局最多80个全局回合或1200条命令。版本、指纹、划分与完整指标摘要已归档：[真实数据实验JSON](benchmarks/real-data-20260922.json)。

- 4局共3404条命令，3局终局、1局因1200条命令上限截断；3个终局均为玩家1获胜。这是数据偏差线索，不能据此评价先手优势或训练价值网络的泛化。
- 编码得到6429个分步样本。训练组种子2203/2201/2202：2391命令、4484分步、1191个真实价值标签；验证组种子2204：1013命令、1945分步、1013个价值标签。这里种子简写只省略共同前缀202609。
- 训练组最长291实体、117候选；验证组最长181实体、117候选。不是固定64实体输入，暂用batch 32，不能把原合成吞吐直接当本轮训练速度。
- 覆盖13类实际命令；没有合成/手绘路径的自然样本，这两类目前只有专项行为测试。扩量时需要针对性补样。
- 原11.55M骨架在同一64样本上训练300/1200步，多候选拟合率分别84.62%/90.38%；1200步仍有落点错误。独立验证策略loss从2.11升至198.83，明确是过拟合，不能用作正式对局模型。
- 诊断时分数绝对值约904，同一检查点的BF16/FP32选择存在差异。v2增加独立候选非线性投影，并保持候选投影/评分头FP32；主干仍BF16。参数11,695,874，约增加1.3%。旧模型/报告保留，网络版本明确校验，拒绝静默部分加载。

v2在相同划分与相同64样本上完成300次有效更新，参数/梯度有限，无跳步；优化循环含抽样、传输与日志约105.75秒，不含前后验证和检查点IO。该时间是一次开发运行，不是与旧网络的受控性能对照。

| 指标                   | 训练前 | 300步后    |
| ---------------------- | ------ | ---------- |
| 64样本策略loss         | 1.7013 | 0.1470     |
| 52个多候选样本拟合率   | 25.00% | 96.15%     |
| 23个多候选根决策拟合率 | 13.04% | 91.30%     |
| 10个已知价值标签MSE    | 1.0543 | 0.00001984 |
| 独立1局验证策略loss    | 2.1110 | 19.1121    |
| 独立验证多候选拟合率   | 23.03% | 27.54%     |

这说明小样本可以被拟合，数据和优化链路能够工作；验证loss恶化表明过拟合，价值样本极少且终局只有玩家1获胜。**此检查点不是正式游戏AI，也不能用于估算作者胜率。**

产物：`encoded/manifest.json`、`encoded/train.pt`、`encoded/validation.pt`、`overfit-v2.pt`及`overfit-v2-300.json`。旧骨架300步快照为`overfit-300.pt`，1200步快照为`overfit.pt`，对应同名前缀JSON报告。编码两份张量合计约619MiB，扩量前必须处理填充与分片成本。

v2另做较大序列规模的速度检查：固定batch 32、256实体、117候选的**合成张量**，CPU 8线程/XPU宿主4线程，各配置独立进程依次执行；完整优化步同步计时。四组全部完成真实更新、无跳步，原始硬件/源码指纹/轮次记录见[新版基准JSON](benchmarks/torch-v2-20260922.json)。

| 设备／主干精度 | 每步中位耗时 | 样本/秒 |
| -------------- | ------------ | ------- |
| CPU FP32       | 1337.34ms    | 23.93   |
| CPU BF16       | 1384.03ms    | 23.12   |
| XPU FP32       | 457.86ms     | 69.89   |
| XPU BF16       | 377.02ms     | 84.88   |

所有配置的候选投影/评分头均FP32。这个规模下XPU BF16约为CPU FP32的3.55倍，继续采用XPU训练；它不包含采样/编码，不是浏览器单次推理延迟，也不能与旧64实体、batch 128的466样本/秒直接比较。

## 浏览器实测：11.7M模型

2026-09-22在本机**Codex内置浏览器**完成ONNX Runtime Web 1.30.0测试，浏览器报告Chrome 153、Intel xe-lpg非软件适配器、shader-f16可用。使用`overfit-v2.pt`及训练组中按实体/候选长度确定的四个真实输入，另将这四个输入合为batch 4；不是八个独立样本。模型、输入、检查点指纹、原生CPU对照和各20次耗时见[浏览器实验JSON](benchmarks/browser-20260922.json)。复现入口见[Python用法](../../training/README.md#浏览器推理验收)。

内置浏览器安全策略拒绝直接打开`file://`。本次使用仅绑定127.0.0.1、仅提供明确列出的测试资产的HTTP服务，未关闭浏览器保护。**这是本地HTTP模型验收，不是离线file://验收，也不是已嵌入发行游戏。**

导出保留动态B/N/A：FP32 ONNX为47,260,927字节，混合FP16为25,401,917字节（约25.4MB），候选投影/评分头仍为FP32。配套asyncify WASM另有26,781,914字节，未压缩；这些体积尚未包含最终单HTML内嵌编码成本。导出器先在原生ONNX Runtime逐组核对PyTorch FP32输出，再由浏览器加载相同文件并校验SHA256。

下表为一次前向的中位耗时；输入张量已准备好，计时包含输入上传和输出回读，排除游戏编码、完整命令解码与MCTS。每形状预热3次、测20次；CPU为WASM单线程、无跨源隔离。实体数不含额外的全局token。

| 批量×实体×候选 | CPU FP32 | WebGPU FP32 | WebGPU混合FP16 |
| -------------- | -------- | ----------- | -------------- |
| 1×4×3          | 5.85ms   | 32.80ms     | 31.10ms        |
| 1×94×26        | 54.20ms  | 46.45ms     | 46.30ms        |
| 1×291×37       | 178.80ms | 81.15ms     | 58.05ms        |
| 1×192×117      | 117.35ms | 51.85ms     | 39.80ms        |
| 4×291×117      | 733.70ms | 151.85ms    | 78.35ms        |

- 数值：所有输出有限；四个不同输入及其合批的top1都与PyTorch FP32相同。混合FP16最大策略概率绝对误差0.002913（约0.291个百分点）、价值误差0.000214、有效logit误差0.0797。只证明这批输入可用，不保证新模型/临界分差下永远不换首选。
- 首次成本单列：页面按CPU FP32→GPU FP32→GPU混合FP16运行；模型加载分别约221/229/98ms，会话创建约485/234/83ms，各自首个小形状前向约25/1234/1026ms。WASM已下载，后两组复用页面运行库；其他新形状还会编译。这不是三个独立冷启动对照。
- 单大局面混合FP16的P95为63.3ms，batch 4为87.2ms。GPU FP32的原始序列存在热身/频率波动，保留原始轮次；本轮为单机探索性测量，没有控制电源/温度，不能外推所有笔记本。
- 独立详细日志确认混合FP16有344个节点分配给WebGPU、54个给CPU；CPU节点包含形状处理和IsNaN，不能称作纯GPU推理。1.30.0的原生WebGPU运行库未提供所配置的JSEP计时回调，本次以节点分配日志确认，没有声称取得逐算子GPU耗时。
- 初始化曾错配JSEP WASM，修正为WebGPU bundle对应的asyncify WASM后重载并重新开始测量；错误启动不计入成绩。CPU计时测试页未移入Worker，不能据此声称已验收游戏UI响应。

据此继续采用**约10–12M起步、保留CPU FP32与WebGPU混合FP16路径**，暂不增至50M。小局面CPU明显更快；大序列GPU受益，批量评估值得继续。完整命令可能有多个参数步骤，当前编码把动作前缀也送入主干，不能直接跨步骤复用主干结果；若调整模型结构再单独验证缓存。MCTS需要批量叶点评估，不能拿单次前向耗时直接当整步延迟。后续以更多局面/临界分差复核精度，测完整命令、Worker响应、峰值内存，再决定INT8或更小蒸馏模型是否必要。

## CLI网络解码与实局验收

本轮交付`src/ai/training/decoder.ts`和`npm run train:match`：共享动作树按网络评分逐参数选择，空分支回溯；只有一个候选时直接推进。Node持有权威引擎与手工教师，常驻Python只接收八项编码张量。规则/schema不符、推理失败、取消或工作量耗尽均明确暂停，不代填end或改用教师。路径、三材料合成、反应所属方、随机预检边界与迟到结果取消有专项测试。回合外可选巨大化抢占调度、浏览器Worker和MCTS尚未接入。

本机CPU FP32、4线程，使用上面的64样本过拟合检查点；对手为easy教师，每决策40节点。种子2026092205–2026092214，每种子两局交换模型席位，共20局；每局最多100个全局回合、1200条命令，每个实际回合200条命令。推理每条完整命令最多32次，墙钟只用于计时和子进程故障保护。

首轮使用64个解码节点，18局终局、2局中断，无非法提交或上限截断；终局均由教师获胜。原记录保留于`artifacts/training/neural-cpu-20260922/`。两次中断分别为种子2026092210、2026092213，模型执玩家1，第56、58条命令之后暂停。均为神行千里（普通21）的“落点→目标”选择：117个落点中分别只有3、4个可继续，前面的高分空分支耗尽预算。仅对原公开局面提高固定预算复现，分别访问81、90个节点后返回合法技能命令，没有修改规则、候选域或模型评分。

因此默认解码预算改为256，并保留暂停时最后尝试的参数前缀。预算仍可能不足以覆盖复杂局面，发生时继续显式暂停；256不是完整动作空间大小或搜索深度保证。新预算的整批复验单独保存于`artifacts/training/neural-cpu256-20260922/`，不覆盖旧实验。

复验20局全部真实终局，无截断、中断或非法提交，共7854条命令，其中2557条由网络方完成，54条为强制反应。网络0胜、教师20胜，说明这个小样本过拟合模型还不具备实用棋力。单次整批运行约435.48秒，含教师思考及记录；网络命令累计约60.80秒。两轮相同种子是重复验证，不能算作40局独立棋力样本。

两轮元数据、完整统计、异常原因、检查点/源码/trace指纹已归档：[CLI网络实验JSON](benchmarks/neural-20260922.json)。对原记录7228条和复验7854条命令逐条重放，前后公开指纹及终局/暂停状态全部一致；两轮共有的7228条命令也完全相同。复验在两处旧暂停局面分别用81、90节点完成同一个合法技能选择，之后正常继续对局。

| 统计范围                   | 完整命令数 | 中位耗时 | P95     | 最大耗时 |
| -------------------------- | ---------- | -------- | ------- | -------- |
| 全部网络命令，含免推理步骤 | 2557       | 15.01ms  | 78.92ms | 239.98ms |
| 实际调用网络的命令         | 1627       | 27.97ms  | 86.31ms | 239.98ms |

实际调用网络的命令平均需要2.01次前向，最多8次；平均36.35ms中，动作树/公开预检占12.41ms，编码0.81ms，Python管道及模型推理22.52ms，其余为观察、调度及提交。进程启动约1.59秒，Python内部加载计时另列；首个真实推理决策也保留在报告中。按每局/实际全局回合累计网络命令耗时，中位128.64ms、P95 530.70ms、最大1.53秒；包含对方回合中的网络反应，但不含对手思考和界面展示等待。

实际编码最大193实体、117候选，网络自然执行11类命令；本批没有自然发生的手绘路径或合成，仍需后续补样。当前单机结果支持继续用约11.7M作为CPU可运行的起点，无需先增加模型规模；下一阶段的主要问题是训练数据与泛化，而非先把参数扩到50M。

计时包含观察、动作树与公开合法性查询、编码、JSON管道、PyTorch前向和引擎提交；模型启动、提交后trace构造/落盘与对手思考另计。强制步骤和真正调用网络的完整命令分别汇总，暂停尝试另列异常，不混入成功命令的延迟分布。这是原生PyTorch CPU延迟，不能当作浏览器WASM/WebGPU或MCTS的整步延迟。

## 下一步与验收门槛

1. **完成M2部署检查**：本地HTTP的动态ONNX/CPU/WebGPU前向已验收；补单HTML打包、允许file://的环境验收、Worker响应、峰值内存和整条分步命令耗时。扩大真实输入与精度边界覆盖，不能把当前四输入实验当完整部署通过。
2. **准备M3数据**：教师自对弈先补到几十局、检查双方终局与能力覆盖；本轮网络对战是流程验收，不能直接冒充教师策略标签。自然数据不足的合成/路径/反应另有标记地补样。用整局验证和多对手评估决定是否扩至5万–20万决策；届时改为分片/长度分桶并测2/4教师进程扩展。训练后的模型用相同固定预算重新对战，并检查空分支回溯是否减少。
3. **M4/M5按原路线推进**：先形成可泛化的教师预训练基线，再接小预算MCTS，随后分轮自对弈与历史对手；最终独立安排作者实战评测。

## 本轮工程验收

此前TypeScript检查、326项行为测试、Python 6项测试、Ruff与格式检查、目录结构检查均通过。构建与deploy:check通过，根HTML和新构建逐字节相同；原游戏34项浏览器验收以**离线file://**通过。该离线结果只验证原游戏发行物；模型浏览器结果为上面的本地HTTP测试。

本轮CLI交付通过TypeScript、332项行为测试、Python 7项测试、Ruff、目录检查和deploy:check内的完整构建，发行HTML仍为807,743字节且逐字节一致。本轮未改页面，未重复浏览器UI验收；不能据此把网络的离线浏览器接入标记完成。新增/修改的TypeScript和文档通过默认Prettier检查；全仓使用相同配置加`--end-of-line auto`检查，避免重写既有Windows CRLF文件。此前默认全仓`format:check`因这些既有行尾报222个文件，记录保留。

## 已有记录

| 日期／提交           | 工作                                       | 证据                                           |
| -------------------- | ------------------------------------------ | ---------------------------------------------- |
| 2026-09-22 / c0d5cd3 | 高速对弈环境、常驻协议、教师样本与Node基准 | [Node报告](TRAINING-NODE-2026-09-22.md)        |
| 2026-09-22 / db2fe7e | Python 3.12/PyTorch骨架与CPU/XPU对照       | [训练性能报告](TRAINING-PYTORCH-2026-09-22.md) |
| 2026-09-22 / f550787 | 真实数据编码、v2小样本训练、浏览器推理基准 | 本文件的真实样本与浏览器实测章节               |

本机Core Ultra 5 225H / Arc 130T / 32GiB内存。固定64实体槽、64候选、batch 128的完整优化步：CPU FP32约96样本/秒，XPU BF16约466样本/秒。它不包含真实编码、采样或MCTS。教师旧基准约6–8条命令/秒，采样可能比训练更新更费时；先测2/4个教师进程的实际扩展，再决定长期采样配置。

## 实验记录要求

每次保留规则/编码/模型版本、源码提交或内容指纹、输入数据指纹、训练/验证整局分组、教师难度和工作量预算、设备精度、训练步数及检查点。报告分开列出：

- 数据：完整/截断/中断局数、真实决策数、分步样本数、动作/棋子覆盖、被拒绝或未支持的样本；不得静默丢失。
- 学习：策略损失、价值损失、有效价值标签数、验证指标、实际优化更新数；截断局不充当平局。
- 对局：对手版本、种子集合、先后手、预算、完整胜负与截断率、非法尝试与强制反应是否完成。
- 性能：采样、编码、加载、训练、推理、完整决策分别计时；首次加载/编译与稳态分开。
- 浏览器：实际file://离线模式、CPU/WebGPU后端、输出差异、峰值内存和批量；生产仍只有一个离线HTML。

当前没有已训练的正式游戏模型，也没有模型对作者胜率数据。

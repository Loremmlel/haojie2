# AI训练进度

本文件持续记录已完成工作、当前实验、复现入口和下一步。最后更新：2026-09-25。训练路线见[TRAINING.md](TRAINING.md)，Python用法见[training/README.md](../../training/README.md)。只有实际运行并通过验收的项目才标记完成。

9月23日补充：[教师采样几何优化与速度对照](performance/TEACHER-2026-09-23.md)。下文40局试采仍属9月22日旧规则，保留原始数据与测量值；当前规则的速度使用新种子、固定预算另测，不能将旧语料改版本号继续编码。

同日追加：[攻击距离场优化](performance/TEACHER-SPATIAL-2026-09-23.md)，在已提交的几何优化上复测单进程采样，保持同预算AI轨迹不变。

同日训练存储改为增量命令轨迹及gzip，旧快照训练JSONL须重新生成；游戏存档兼容不变。编码、审计、网络报告共用重放入口，prepare经管道直接保存`.pt`。实施与体积实测见[训练记录精简](../plans/TRAINING-RECORDS-2026-09-23.md)。历史实验数值不改写，不将存储缩减称作棋力或训练吞吐提升。

## 目标与约束

- 首个交付目标：真实公开局面编码 → 教师样本 → 策略/价值训练 → 合法完整对局 → 离线浏览器推理。
- 继续接入含规则随机事件的MCTS与分轮自对弈；对作者超过80%胜率保留为独立实战目标，不用训练loss、固定种子自对弈或短局代替。
- 先经典模式打通；神龛状态须能编码，暗选搜索另做信息边界验收。双方手牌公开，正式seed/rng不进入网络或搜索。
- 引擎、几何、能力与合法性查询继续复用TypeScript；训练在仓库内独立Python环境进行。模型、数据、临时报告放artifacts/training，源码与小型实验摘要入Git。
- 当前约11.70M网络，Intel机器使用XPU BF16，RTX 3060 Laptop机器使用CUDA BF16；均保留FP32候选评分、batch 32起步，CPU FP32作为参照。原11.55M/64实体合成基准中的batch 128不是当前真实数据的固定配置。

## 阶段状态

| 阶段                            | 状态       | 验收与下一步                                                                              |
| ------------------------------- | ---------- | ----------------------------------------------------------------------------------------- |
| M0 高速环境、模型骨架和设备基准 | 完成       | 常驻Node环境、教师JSONL、11.55M骨架、保存续训、18组CPU/XPU精度基准                        |
| M1 真实状态与动作编码           | 首版完成   | 79条正式回放逐条结算一致；路径/材料/反应/暗选/大局面测试；3404个教师命令全部转换，无丢样  |
| M2 少量真实数据训练与部署可行性 | 进行中     | 64样本拟合、ONNX导出、浏览器本地HTTP前向、20局完整CLI网络对战通过；离线打包与Worker待完成 |
| M3 第一轮教师预训练             | 方法已筛查 | 完成规模、空间和学习率/dropout对照，尚无稳定泛化收益；下一步转向搜索监督增益              |
| M4 小预算MCTS                   | 续演已筛查 | 真实终局短续演在人工夹具60/60；普通自然位置仍无信号，需验证根覆盖及连续决策兑现获胜       |
| M5 分轮自对弈与最终实战         | 待完成     | 历史对手、败局/罕见能力补样、固定预算的开发评测；最终作者测试独立保留                     |

M1设计：主棋子、手牌、基地及其附属状态分别为实体记录；来源和身份映射到局内引用，不解析原始ID中的序列号。模型使用分步动作选择，路径逐格延伸、材料逐个选择；公开参数域与完整合法命令分开标识，不能把教师裁剪候选宣称为完整动作空间。

具体特征、标签和解码边界见[编码说明](../../training/ENCODING.md)，可复现命令见[Python用法](../../training/README.md)。M1首版通过当前回放和专项局面，不能推断已覆盖所有罕见能力组合。

## 当前实验：真实样本流程检查

目录：`artifacts/training/pilot-20260922/`。经典模式、easy教师、每决策40工作量节点、种子2026092201–2026092204，每局最多80个全局回合或1200条命令。版本、指纹、划分与完整指标摘要已归档：[真实数据实验JSON](benchmarks/real-data-20260922.json)。

- 4局共3404条命令，3局终局、1局因1200条命令上限截断；3个终局均为玩家1获胜。这是数据偏差线索，不能据此评价先手优势或训练价值网络的泛化。
- 编码得到6429个分步样本。训练组种子2203/2201/2202：2391命令、4484分步、1191个真实价值标签；验证组种子2204：1013命令、1945分步、1013个价值标签。这里种子简写只省略共同前缀202609。
- 训练组最长291实体、117候选；验证组最长181实体、117候选。不是固定64实体输入，暂用batch 32，不能把原合成吞吐直接当本轮训练速度。
- 覆盖13类实际命令；没有合成/手绘路径的自然样本，这两类目前只有专项行为测试。扩量时需要针对性补样。
- 原11.55M骨架在同一64样本上训练300/1200步，多候选拟合率分别84.62%/90.38%；1200步仍有落点错误。独立验证策略loss从2.11升至198.83，明确是过拟合，不能用作正式对局模型。
- 诊断时分数绝对值约904，同一检查点的BF16/FP32选择存在差异。v2增加独立候选非线性投影，并保持候选投影/评分头FP32；主干仍BF16。参数11,695,874，约增加1.3%。旧模型/报告保留，网络版本明确校验，拒绝静默部分加载。

v2在相同划分与相同64样本上完成300次有效更新，参数/梯度有限，无跳步；优化循环含抽样、传输与日志约105.75秒，不含前后验证和检查点IO。该时间是一次开发运行，不是与旧网络的受控性能对照。

| 指标                   | 训练前 | 300步后    |
| ---------------------- | ------ | ---------- |
| 64样本策略loss         | 1.7013 | 0.1470     |
| 52个多候选样本拟合率   | 25.00% | 96.15%     |
| 23个多候选根决策拟合率 | 13.04% | 91.30%     |
| 10个已知价值标签MSE    | 1.0543 | 0.00001984 |
| 独立1局验证策略loss    | 2.1110 | 19.1121    |
| 独立验证多候选拟合率   | 23.03% | 27.54%     |

这说明小样本可以被拟合，数据和优化链路能够工作；验证loss恶化表明过拟合，价值样本极少且终局只有玩家1获胜。**此检查点不是正式游戏AI，也不能用于估算作者胜率。**

产物：`encoded/manifest.json`、`encoded/train.pt`、`encoded/validation.pt`、`overfit-v2.pt`及`overfit-v2-300.json`。旧骨架300步快照为`overfit-300.pt`，1200步快照为`overfit.pt`，对应同名前缀JSON报告。编码两份张量合计约619MiB，扩量前必须处理填充与分片成本。

v2另做较大序列规模的速度检查：固定batch 32、256实体、117候选的**合成张量**，CPU 8线程/XPU宿主4线程，各配置独立进程依次执行；完整优化步同步计时。四组全部完成真实更新、无跳步，原始硬件/源码指纹/轮次记录见[新版基准JSON](benchmarks/torch-v2-20260922.json)。

| 设备／主干精度 | 每步中位耗时 | 样本/秒 |
| -------------- | ------------ | ------- |
| CPU FP32       | 1337.34ms    | 23.93   |
| CPU BF16       | 1384.03ms    | 23.12   |
| XPU FP32       | 457.86ms     | 69.89   |
| XPU BF16       | 377.02ms     | 84.88   |

所有配置的候选投影/评分头均FP32。这个规模下XPU BF16约为CPU FP32的3.55倍，继续采用XPU训练；它不包含采样/编码，不是浏览器单次推理延迟，也不能与旧64实体、batch 128的466样本/秒直接比较。

### 另一台电脑的CUDA训练补测

2026-09-22在Ryzen 7 5800H／RTX 3060 Laptop 6GiB电脑安装Python 3.12.10、PyTorch 2.14.0+cu130。相同v2模型、batch 32、256实体、117候选的初始权重与输入SHA256均与上表一致。同口径短测：CUDA FP32/BF16/FP16分别143.47/305.24/336.18样本/秒，本机CPU FP32为12.72样本/秒。CUDA BF16比上表历史XPU BF16快3.60倍、历史CPU FP32快12.76倍；不能套用旧11.55M小输入的466样本/秒作对照。

延长为每精度250次计时更新后，BF16/FP16约339.56/337.19样本/秒，两者无稳定优劣结论，均无跳步、参数与梯度有限。batch 32的混合精度峰值allocated约945MiB，batch 64及348实体／212候选也完成短测。较大输入BF16初始top1有1/32与FP32不同，正式训练继续保留FP32验证。CUDA训练保存10步后另起进程续训至20步已通过；这不是正式模型训练或棋力验收。

本机保留CUDA BF16、batch 32作为起点，batch 64待真实变长数据验证。完整硬件／方法／温度功耗边界、原始记录、安装与复现见[CUDA训练报告](TRAINING-PYTORCH-2026-09-22.md#cuda补测rtx-3060-laptop)。桌面负载和频率未锁定，GPU提速不代表Node采样或整个训练流程同倍加速。

## 浏览器实测：11.7M模型

2026-09-22在本机**Codex内置浏览器**完成ONNX Runtime Web 1.30.0测试，浏览器报告Chrome 153、Intel xe-lpg非软件适配器、shader-f16可用。使用`overfit-v2.pt`及训练组中按实体/候选长度确定的四个真实输入，另将这四个输入合为batch 4；不是八个独立样本。模型、输入、检查点指纹、原生CPU对照和各20次耗时见[浏览器实验JSON](benchmarks/browser-20260922.json)。复现入口见[Python用法](../../training/README.md#浏览器推理验收)。

内置浏览器安全策略拒绝直接打开`file://`。本次使用仅绑定127.0.0.1、仅提供明确列出的测试资产的HTTP服务，未关闭浏览器保护。**这是本地HTTP模型验收，不是离线file://验收，也不是已嵌入发行游戏。**

导出保留动态B/N/A：FP32 ONNX为47,260,927字节，混合FP16为25,401,917字节（约25.4MB），候选投影/评分头仍为FP32。配套asyncify WASM另有26,781,914字节，未压缩；这些体积尚未包含最终单HTML内嵌编码成本。导出器先在原生ONNX Runtime逐组核对PyTorch FP32输出，再由浏览器加载相同文件并校验SHA256。

下表为一次前向的中位耗时；输入张量已准备好，计时包含输入上传和输出回读，排除游戏编码、完整命令解码与MCTS。每形状预热3次、测20次；CPU为WASM单线程、无跨源隔离。实体数不含额外的全局token。

| 批量×实体×候选 | CPU FP32 | WebGPU FP32 | WebGPU混合FP16 |
| -------------- | -------- | ----------- | -------------- |
| 1×4×3          | 5.85ms   | 32.80ms     | 31.10ms        |
| 1×94×26        | 54.20ms  | 46.45ms     | 46.30ms        |
| 1×291×37       | 178.80ms | 81.15ms     | 58.05ms        |
| 1×192×117      | 117.35ms | 51.85ms     | 39.80ms        |
| 4×291×117      | 733.70ms | 151.85ms    | 78.35ms        |

- 数值：所有输出有限；四个不同输入及其合批的top1都与PyTorch FP32相同。混合FP16最大策略概率绝对误差0.002913（约0.291个百分点）、价值误差0.000214、有效logit误差0.0797。只证明这批输入可用，不保证新模型/临界分差下永远不换首选。
- 首次成本单列：页面按CPU FP32→GPU FP32→GPU混合FP16运行；模型加载分别约221/229/98ms，会话创建约485/234/83ms，各自首个小形状前向约25/1234/1026ms。WASM已下载，后两组复用页面运行库；其他新形状还会编译。这不是三个独立冷启动对照。
- 单大局面混合FP16的P95为63.3ms，batch 4为87.2ms。GPU FP32的原始序列存在热身/频率波动，保留原始轮次；本轮为单机探索性测量，没有控制电源/温度，不能外推所有笔记本。
- 独立详细日志确认混合FP16有344个节点分配给WebGPU、54个给CPU；CPU节点包含形状处理和IsNaN，不能称作纯GPU推理。1.30.0的原生WebGPU运行库未提供所配置的JSEP计时回调，本次以节点分配日志确认，没有声称取得逐算子GPU耗时。
- 初始化曾错配JSEP WASM，修正为WebGPU bundle对应的asyncify WASM后重载并重新开始测量；错误启动不计入成绩。CPU计时测试页未移入Worker，不能据此声称已验收游戏UI响应。

据此继续采用**约10–12M起步、保留CPU FP32与WebGPU混合FP16路径**，暂不增至50M。小局面CPU明显更快；大序列GPU受益，批量评估值得继续。完整命令可能有多个参数步骤，当前编码把动作前缀也送入主干，不能直接跨步骤复用主干结果；若调整模型结构再单独验证缓存。MCTS需要批量叶点评估，不能拿单次前向耗时直接当整步延迟。后续以更多局面/临界分差复核精度，测完整命令、Worker响应、峰值内存，再决定INT8或更小蒸馏模型是否必要。

## CLI网络解码与实局验收

本轮交付`src/ai/training/decoder.ts`和`npm run train:match`：共享动作树按网络评分逐参数选择，空分支回溯；只有一个候选时直接推进。Node持有权威引擎与手工教师，常驻Python只接收八项编码张量。规则/schema不符、推理失败、取消或工作量耗尽均明确暂停，不代填end或改用教师。路径、三材料合成、反应所属方、随机预检边界与迟到结果取消有专项测试。回合外可选巨大化抢占调度、浏览器Worker和MCTS尚未接入。

本机CPU FP32、4线程，使用上面的64样本过拟合检查点；对手为easy教师，每决策40节点。种子2026092205–2026092214，每种子两局交换模型席位，共20局；每局最多100个全局回合、1200条命令，每个实际回合200条命令。推理每条完整命令最多32次，墙钟只用于计时和子进程故障保护。

首轮使用64个解码节点，18局终局、2局中断，无非法提交或上限截断；终局均由教师获胜。原记录保留于`artifacts/training/neural-cpu-20260922/`。两次中断分别为种子2026092210、2026092213，模型执玩家1，第56、58条命令之后暂停。均为神行千里（普通21）的“落点→目标”选择：117个落点中分别只有3、4个可继续，前面的高分空分支耗尽预算。仅对原公开局面提高固定预算复现，分别访问81、90个节点后返回合法技能命令，没有修改规则、候选域或模型评分。

因此默认解码预算改为256，并保留暂停时最后尝试的参数前缀。预算仍可能不足以覆盖复杂局面，发生时继续显式暂停；256不是完整动作空间大小或搜索深度保证。新预算的整批复验单独保存于`artifacts/training/neural-cpu256-20260922/`，不覆盖旧实验。

复验20局全部真实终局，无截断、中断或非法提交，共7854条命令，其中2557条由网络方完成，54条为强制反应。网络0胜、教师20胜，说明这个小样本过拟合模型还不具备实用棋力。单次整批运行约435.48秒，含教师思考及记录；网络命令累计约60.80秒。两轮相同种子是重复验证，不能算作40局独立棋力样本。

两轮元数据、完整统计、异常原因、检查点/源码/trace指纹已归档：[CLI网络实验JSON](benchmarks/neural-20260922.json)。对原记录7228条和复验7854条命令逐条重放，前后公开指纹及终局/暂停状态全部一致；两轮共有的7228条命令也完全相同。复验在两处旧暂停局面分别用81、90节点完成同一个合法技能选择，之后正常继续对局。

| 统计范围                   | 完整命令数 | 中位耗时 | P95     | 最大耗时 |
| -------------------------- | ---------- | -------- | ------- | -------- |
| 全部网络命令，含免推理步骤 | 2557       | 15.01ms  | 78.92ms | 239.98ms |
| 实际调用网络的命令         | 1627       | 27.97ms  | 86.31ms | 239.98ms |

实际调用网络的命令平均需要2.01次前向，最多8次；平均36.35ms中，动作树/公开预检占12.41ms，编码0.81ms，Python管道及模型推理22.52ms，其余为观察、调度及提交。进程启动约1.59秒，Python内部加载计时另列；首个真实推理决策也保留在报告中。按每局/实际全局回合累计网络命令耗时，中位128.64ms、P95 530.70ms、最大1.53秒；包含对方回合中的网络反应，但不含对手思考和界面展示等待。

实际编码最大193实体、117候选，网络自然执行11类命令；本批没有自然发生的手绘路径或合成，仍需后续补样。当前单机结果支持继续用约11.7M作为CPU可运行的起点，无需先增加模型规模；下一阶段的主要问题是训练数据与泛化，而非先把参数扩到50M。

计时包含观察、动作树与公开合法性查询、编码、JSON管道、PyTorch前向和引擎提交；模型启动、提交后trace构造/落盘与对手思考另计。强制步骤和真正调用网络的完整命令分别汇总，暂停尝试另列异常，不混入成功命令的延迟分布。这是原生PyTorch CPU延迟，不能当作浏览器WASM/WebGPU或MCTS的整步延迟。

## 下一步与验收门槛

1. **完成M2部署检查**：本地HTTP的动态ONNX/CPU/WebGPU前向已验收；补单HTML打包、允许file://的环境验收、Worker响应、峰值内存和整条分步命令耗时。扩大真实输入与精度边界覆盖，不能把当前四输入实验当完整部署通过。
2. **推进M3预训练**：价值目标、席位分布和常数基线已核查；9月25日[消融](experiments/VALUE-ABLATION-2026-09-25.md)和[扩量](experiments/EXPANSION-2026-09-25.md)没有明显策略泛化收益。[诊断](experiments/DIAGNOSTIC-2026-09-25.md)定位部署学习薄弱；[采样对照](experiments/SAMPLING-2026-09-25.md)确认训练分配有影响，但价值误差变差。[专项曲线](experiments/DEPLOYMENT-FIT-2026-09-25.md)显示过拟合；[空间结构](experiments/SPATIAL-ABLATION-2026-09-25.md)改善拟合效率，[数据规模](experiments/DATA-SIZE-2026-09-25.md)有条件地改善验证，两者收益均不稳定。最新[论文与实现调查](research/TRAINING-AND-SEARCH-2026-09-25.md)将下一步扩展为固定数据的学习率/dropout对照、相对位置与候选表示调查，以及有界MCTS搜索增益探针；不再以教师Top-1作为唯一目标或搜索启动门槛。同配置复现差异、教师战术质量和合成/路径专项正例仍待核查。当前5族是开发验证，最终模型选择仍需独立局面和新种子换边实战；现有候选不发行，MCTS、自对弈闭环与浏览器模型接入尚未验收。
3. **M4/M5按原路线推进**：先形成可泛化的教师预训练基线，再接小预算MCTS，随后分轮自对弈与历史对手；最终独立安排作者实战评测。

## M3方案与教师探索

2026-09-22讨论建议：困难AI可以作为启动教师的候选，但还没有实验能证明它达到作者水平，或适合成为最终棋力标准。当前困难档是手工评分、宽度2/最多7层动作链的受限束搜索，加少量对手回应；不是搜索7个完整回合。生产配置单次节点目标上限800，实际预算还受回合分配影响。增加节点不会自动扩大宽度、动作候选或搜索层数，不能假定成本翻倍就一定更强。前面的20局对手是easy/40节点，不构成困难教师的质量验收。

建议分三步推进：

1. **试采并审计教师**：先40–60局，记录真实终局、双方胜负、罕见能力、截断及时间；另用相同种子和交换席位比较困难与中等，用公开战术局面检查可完成击杀、反伤/反制、召唤收尾、路径和合成选择。对少量相同局面比较固定work预算800/1600，确认更高预算的实际收益，再选择批量采样配置。数字是试验起点，不是已验证的最优设置。
2. **扩大局面来源**：采样权重先试70%强教师主导的正常对局、20%混合对手及学生实际到达的局面、10%稀有能力/合法残局。比例按被选中的完整命令计，不按局数计；先形成1万–3万条含多候选步骤的命令，再按独立验证结果扩到5万–20万。弱对手或探索动作可用于到达新局面，策略标签应由合格教师重新查询，不能把探索动作当正确答案。专项局面须可由引擎合法到达或经完整状态校验，教师不会的能力要用更充分搜索或已验证的战术解补标签，不能把“可施放”直接标成“好动作”。
3. **正式预训练并评测**：保留11.7M、XPU BF16主干/FP32候选头、batch 32，从新初始化开始；不续训64样本过拟合检查点。先比较少量1e-4/3e-4学习率试验，每1–3个等效数据遍历保存/验证，再根据走势继续。策略用教师选择，价值只用真实终局、按决策所属方记收益；截断/未完成分支不伪造胜负。训练/验证/测试按整局种子族划分，交换席位、同源分支和重标版本必须留在同组，不能随机拆散分步样本。

训练中分开看多候选根/参数步骤表现、完整命令与教师的一致性、实战胜负、空分支回溯及中断率。单候选节点不抬高策略准确率；长路径和长局的采样权重需要控制。教师有多个近似好动作时不强求100%模仿。混合对手轨迹的终局标签反映该续局策略分布，不是对困难档或作者的统一胜率；保留来源，并分别检查正常对局和专项样本指标。当前小样本模型的0胜20负只说明该检查点泛化很差，不能据此判断无人工棋谱路线失败。

本轮已接入混合教师、相同种子交换席位、逐命令计时与明确中断报告。prepare区分轨迹game_id与规则/模式/种子group：不同轨迹可以共用种子族，重复轨迹拒收，整个种子族不能跨训练/验证。新增按教师难度筛选并报告排除数量，不能直接将默认混合文件全部标为hard示范。扩量前仍缺分片/长度分桶、公开局面重标、周期检查点与独立测试集。单/双/四进程的受控吞吐对照尚未测，不能把easy/40节点的旧速度外推到困难教师。

没有人类棋谱不构成方法上的障碍：[AlphaGo Zero原论文](https://www.nature.com/articles/nature24270)展示了无人工对局数据的自对弈学习；但其游戏条件、算力和样本量不能直接套到本项目。我们已有的手工AI可提供启动经验，主要风险是教师盲区、局面分布狭窄、随机终局噪声和有限算力。后续让学生访问新局面，再查询教师纠错，借鉴[DAgger](https://proceedings.mlr.press/v15/ross11a.html)处理模仿学习中的分布偏移；再由更强搜索产生新目标，按[Expert Iteration](https://papers.nips.cc/paper_files/paper/2017/hash/d8e1344e27a5b08cdfd5d027d9b8d6de-Abstract.html)的思路迭代。上述是建议路线，未实现这些论文的完整算法，也不保证最终对作者80%胜率。

## M3教师试采结果

目录`artifacts/training/teacher-pilot-20260922/`。完成20个新种子2026092230–2026092249，每个种子交换席位，共40局；经典模式、hard/800对medium/320，每局上限100个全局回合或3000条命令。四个Node进程各负责连续5个种子。源码与原始数据指纹、逐局结果、编码检查和预算审计保存在[教师试采摘要](benchmarks/teachers-20260922.json)。这批是开发语料，不能再作为最终未见测试集。

37局真实终局、3局达到回合上限截断，无中断或非法提交。37516条命令的公开Observation、真实结算、命令后指纹和终局/截断状态逐条重放一致。困难方25胜、12负；三局截断保留未知收益，没有判成平局。两席位均已终局的完整种子对有17对，余3对不补成完整胜负。

| 困难教师席位 | 胜  | 负  | 截断 |
| ------------ | --- | --- | ---- |
| 玩家1／先手  | 18  | 1   | 1    |
| 玩家2／后手  | 7   | 11  | 2    |

这是两个不同难度及不同预算的教师配置比较，没有分离算法与计算量的贡献，也不是对作者胜率。结论是**hard/800可以作为启动教师，但席位差异明显，仍需补足能力覆盖并通过后续搜索迭代提高标签质量**。

所有命令均通过共享动作树与输入编码器，无丢样，共72736个分步。hard来源有19499条命令、38109个分步，其中16978条命令含多候选步骤、16972个根决策含多候选；3646个分步只有一个候选。16070个hard根决策有真实终局收益（11154胜、4916负），3429个来自截断局的根决策没有价值标签。正式数据用`--teacher-difficulty hard`排除18017条medium命令，不排除hard败局。

hard输入最长348实体、212候选，平均约119.60实体；全批最大360实体。候选数已经超过早先基准的117，不能增加固定截断。若把全部hard分步按全局最大形状填充，FP32/Int64/Bool张量元素约5.76GB；可变长元素约1.39GB。这里未计元数据、偏移和序列化，现有prepare按训练/验证分别填充，实际文件大小会不同；该估算支持先做变长存储与分桶。

自然数据覆盖14类命令，hard实际主动技能来自13个棋种；没有合成成功或显式手绘路径样本。进一步枚举实际合成窗口的公开材料与合法落点：hard的13次窗口决策都存在合法合成，最终全部跳过，涉及3局而非13个独立对局。这不证明跳过是坏棋，但明确说明模仿这批数据无法得到合成正例。下一轮应补“合成确实值得做”的可验证战术情形及路径示范，不能只给所有可合成局面强贴合成标签。

预算审计已完成：从旧easy/40语料的227个合格位置中按公开指纹哈希选择32个，同位置分别重新决策并重复一次。三个配置的96次确定性复查、公开预检与输入不变检查通过。800→1600有9处更换命令；实际采用完整对手回应的位置由14/32增至29/32，预算耗尽由25/32降至11/32。首次决策累计耗时约92.77秒与134.81秒，但当时仍有采样与开发负载，不能作为严格性能对照；换招也不等于改进。没有提高束宽或动作链深度，暂不全量改用1600。

数据准备先在已完成的两对种子2230/2231上验收：4局2986条命令全部重放一致；按hard来源保留1554条命令、排除1432条medium命令，得到3120个分步样本。2231的两个席位一起进入训练组（545命令/1074分步），2230的两个席位一起进入验证组（1009命令/2046分步）；价值标签只在1554个根决策启用。验证组最长348实体，仍不能按固定64实体估算训练成本。这只是预处理验收，尚未执行正式训练。

本批从首个采样开始到最后一份报告落盘约99.28分钟，约6.30条完整命令/秒，原始JSONL共762.34MB；包含缓存命令。最长一局约35.12分钟。各进程累计对局耗时中约99.77%是教师计算，权威引擎提交平均约0.386ms/命令。hard非缓存且实际使用模拟节点的9522次决策，中位726.60ms、P95 6572.26ms；缓存占9977条命令，不能用含缓存的极低中位数冒充搜索速度。四进程与开发检查同时运行，后段还存在静态分组的空闲，因此这些是本机采样成本，不是受控单/双/四进程扩展基准。近期按万级决策样本推进更合适；几十万局不应直接套用当前全手工教师的成本。

另在前两对已重放种子中，选取hard实际非缓存play决策里棋子最多的公开位置（2230换边局、第1641条命令前，61棋子），用Node V8默认1000微秒采样检查两个相同冷计划。首个计划耗时10.36秒，800节点耗尽、346候选、未采用对手回应；重复决策一致。搜索栈内加权叶帧占比：`geometry.cells`约34.15%、`structuredClone`约8.64%、`attackPath`约8.21%、AI的`attackField`约6.97%。这些是单个密集局面、含剖析与并发干扰的热点线索，不是全语料的平均CPU占比。下一轮优先试验占位格/攻击查询的重复分配与计算优化，并对比命令及概率结果；本轮没有修改引擎或教师算法，也没有据此承诺加速倍数或移植语言。

## 2026-09-24 当前规则再采样与短训

本机目录`artifacts/training/teacher-feedback5-20260924/`保存当前规则的40局新轨迹、审计、张量、两组检查点和报告。经典模式，20个新种子各交换席位，hard/800对medium/320；36局真实终局、4局截断、0局中断，共37352条命令。六份轨迹并发重放与编码审计通过；按hard来源保留19956个根决策、39088个分步，其中15306个根决策有真实终局价值标签。训练/验证按整种子族分成15/5组，分别为28558/10530个分步，不把换边局拆开。

采样脚本已改为按种子对动态分发，供后续新采样使用；本批40局由此前的静态六进程完成，不能把动态调度器说成这批采样的加速来源。多文件审计改为六个独立Node进程，实际耗时约9分09秒。预处理会再次重放并生成张量，已改为按文件同时启动六个Node编码器，由Python收集张量后统一校验、分组、填充、写盘；本批预处理约9分39秒，输出文件约3.53GB/1.11GB。串行版在第二份文件处理中被切换，没有完整串行基线，不能据此宣称精确加速倍数。

同一批数据从新初始化的11.7M模型分别做CUDA BF16、batch 32、1000次有效更新的短训，参数和梯度有限；验证集训练前根决策准确率约10.8%、策略损失2.052、价值MSE 1.215。学习率1e-4训练后分别为29.8%、1.577、2.118；3e-4为27.5%、1.647、2.130。1e-4的策略指标略优，但两组价值泛化都恶化，训练集价值MSE已降到0.028/0.017，表明严重过拟合风险。两份检查点只是短训诊断，不是已选定的游戏模型；浏览器和离线发行仍未验收。下一步需检查价值目标及席位/终局分布，增加独立测试和更合理的张量分片/长度分桶，再决定继续训练的配置。

随后核对终局价值标签：训练集胜/负根决策为8929/3018，验证集为1035/2324；胜局占比分别约74.7%/30.8%。验证集hard执后手的四局终局全部失败，这种按完整种子组划分后出现的分布差异可能放大价值误差，但不证明它是唯一原因，也不能为了提高指标反复挑选划分。用未参与训练的种子2026092451–2455各交换网络席位，与easy/40进行10局完整CLI对战：网络0胜、教师10胜；10局均真实终局，无截断、中断或非法提交。原生CPU FP32推理与对局报告保存在`artifacts/training/teacher-feedback5-20260924/heldout-easy-10/`。这只验证当前贪心分步解码检查点的有限新种子表现，不能外推对作者胜率。应先做价值目标/席位分布诊断、扩充并保留独立测试数据，再决定下一轮预训练；此检查点不作为MCTS棋力基线。

## 2026-09-24 模型结构与训练规模取舍

对“胜任需要512～1024宽、12～24层、50M～200M参数、百万至千万局自对弈和多卡训练”的判断：这些是可研究的方向，**不是由游戏复杂度直接推出的必要门槛**。当前11.7M网络已拟合训练价值、但验证价值恶化且未见种子对easy/40为0胜10负；现有实验尚不能区分容量不足、标签/划分偏差、教师盲区和贪心解码的贡献。在同一批40局上直接扩大网络，不能视为已解决泛化或棋力问题。按当前`PolicyValueNet`的实际结构，512宽/12层/2048 FFN约39.6M参数，1024宽/24层/4096 FFN约309.0M参数，并非所提范围必然落在50M～200M；本机CUDA显存6GiB，目标还包括CPU与单文件离线浏览器推理，训练显存、完整命令延迟及发布体积都须实测。

- **空间结构**：公开编码已有棋子及候选的绝对坐标、派生属性、装备、效果、蓄力和公开手牌，但没有显式的两实体相对距离或空间注意力偏置。先用共享规则查询构造可验证的公开关系特征，或对相对位置偏置做小规模对照；图注意力不是预设答案。改变编码语义须升级版本并重新预处理原轨迹，不能静默复用旧张量。
- **搜索**：优先在精确引擎上验证小预算MCTS，机会节点遵守真实概率，连续命令与强制反应按实际决策方回传，分步组合动作控制扩展和推理成本；与纯网络在相同新种子、换边和工作量下比较。Gumbel AlphaZero列为少量模拟的后续对照。已知规则转移不因状态复杂就先改用MuZero学习转移；ReBeL/CFR只在神龛暗选等隐藏信息问题确实需要时评估，不套用于公开信息的经典模式。搜索工程可独立开发，但当前价值头不作为已验证的MCTS棋力基线。
- **特征与样本**：优先检查攻击可达性、遮挡、相对位置及稀有合成/路径示范的缺口；不得把正式seed/rng、未知牌序或未揭示暗选送入网络。先修正价值目标和席位/终局分布的评估方法，保留整种子族隔离与未见测试，再逐级扩量和比较模型容量。百万至千万局是某些大规模自对弈系统的实验规模，不是本游戏的固定入场条件；是否需要多卡由测得的样本效率、吞吐、显存和目标设备延迟决定。

下一轮的判据是：同数据/预算下比较当前模型与逐级扩容模型，报告分席位的独立价值误差、策略表现和完整对局结果；价值须与简单常数基线比较，不能仅看训练损失。若数据或搜索吞吐仍是主要瓶颈，优先改进这些环节，不预先承诺50M以上或百万局规模。相关研究可参照[相对位置注意力](https://aclanthology.org/N18-2074/)、[Gumbel AlphaZero](https://openreview.net/pdf?id=bERaNdoegnO)、[MuZero](https://www.nature.com/articles/s41586-020-03051-4)与[ReBeL](https://arxiv.org/abs/2007.13544)；论文结论不等同于本游戏实测收益。

## 2026-09-25 价值基线、权重消融与延长训练

完整条件、指纹、复现与限制见[实验报告](experiments/VALUE-ABLATION-2026-09-25.md)，产物位于`artifacts/training/value-ablation-20260925/`。本轮复用40局当前规则语料和原15/5种子族划分，未重新挑选验证集。新增按席位/整局的价值诊断和仅从训练标签拟合的常数基线；训练支持显式价值损失权重，检查点防止跨目标续训，旧检查点按权重1兼容。

原1000步模型的FP32验证价值MSE为2.11787，差于零常数1.00000和训练分席位均值基线1.48177。保持11.7M模型、初始化/抽样种子、学习率1e-4和batch 32，仅将价值权重改为0或0.1各训练1000步，再将纯策略检查点续训到4000步。全部完成真实更新、无跳步、数值有限；原检查点保留。

| 配置                    | FP32验证根准确率 | FP32验证价值MSE | 新种子对easy/40 |
| ----------------------- | ---------------: | --------------: | --------------- |
| 权重1／1000步（原对照） |           29.81% |         2.11787 | 0胜10负         |
| 权重0／1000步           |           29.20% |          不训练 | 0胜10负         |
| 权重0.1／1000步         |           29.44% |         1.00023 | 0胜10负         |
| 权重0／4000步           |           29.73% |          不训练 | 0胜9负、1截断   |

实战使用2026092501–2505各交换席位，四组共40次运行、14310条命令重放通过，无非法提交或异常中断。4000步组先手种子2503到1200条命令上限截断，保留未知结果；这批是5个种子族的配对开发比较，不是40个独立测试种子。网络CPU FP32贪心分步解码，不使用价值头；MCTS仍未接入。

降低价值权重能缓解这次价值泛化恶化，但仍未优于零常数；延长训练主要改善训练集，未见明显策略泛化或棋力收益。保留0.1为后续候选，不修改默认训练权重。下一步优先扩大整种子族、学生访问局面的教师重标与罕见能力正例，再比较按完整命令/整局平衡采样；暂不以增大模型或重复这40局训练代替数据与解码诊断。该轮结束时分片/长度分桶、公开局面重标和MCTS尚未完成；同日后续工具与运行进展见下节。

本轮Python 9项测试、366项行为测试、格式/类型/目录检查、构建、deploy:check以及现有Chrome下file://断网生产HTML与Pages/嵌入验收通过。训练模型尚未嵌入发行HTML，不能把页面验收当成网络部署验收。

## 2026-09-25 同日后续：新局面与教师重标已评估

本轮数据扩充已完成，完整固定条件、工具边界及结果见[扩量实验](experiments/EXPANSION-2026-09-25.md)。新增16局hard/800对medium/320教师对战，种子2026092521–2528换边，4进程动态分发；14局终局、2局回合上限截断、0中断，共18102条命令。另从上一轮4000步学生轨迹完成165个公开局面的hard/800冷计划重标，其中139个命令不同；全部仅用于策略监督，不继承学生胜负，不把分歧称为已证实的错误。

公开局面重标、逐分片SHA256/mmap读取、长度分桶和固定原种子族划分已经实现并通过行为测试。正式训练集为28族、47152分步、23881根决策、19098个价值标签；其中341个纠错分步、165根决策全部零价值遮罩。旧学生评测种子2501–2505纳入训练，2581–2585用于本轮候选和旧模型换边对照，与训练/验证族无交集。旧验证5族、10530分步、5450根决策、3359个价值标签保持不变，全部验证张量与旧版逐元素相同。

产物`artifacts/training/expansion-20260925/`，流水线于11:36:53（UTC+8）正常完成。CUDA BF16完成2000次真实更新，参数/梯度有限。FP32验证策略loss为1.57670，多候选根准确率29.33%（旧模型29.44%），价值MSE为0.83955（旧模型1.00023，零常数1.0）；但整局等权MSE仍为1.03750，尚不足以证明价值头可用于搜索。新旧模型各10局对easy/40均0胜10负、全部终局、无截断/中断/非法提交；20局共用5个独立种子族，6639条命令再次重放通过。完成汇总后暂停本轮自动化，没有升级发行模型或自动提交、推送。

本轮扩量同时改变数据、长度分桶和训练步数，不能把价值误差下降单独归因于纠错样本，也没有策略或实战改善证据。下一步优先做败局决策诊断及固定配置的有/无纠错对照。一般混合教师轨迹的重标、按完整命令/整局平衡、稀有合成/路径专项正例和MCTS仍待后续验证。工程入口此前通过367项行为测试、Python 9项测试、类型/格式/目录检查及构建、deploy:check；本次收尾只更新文档并核对产物。

## 2026-09-25 败局诊断与纠错对照

接续扩量实验，完成[败局诊断与纠错对照](experiments/DIAGNOSTIC-2026-09-25.md)，产物为`artifacts/training/diagnostic-20260925/`。30个预先按部署/局中/末段选取的位置均重现学生命令，hard/800与hard/1600共60个教师命令全部被同一动作树覆盖，两档教师的30处选招完全相同。21处分歧中12处首先发生在落点、9处在根动作；这支持先检查学生排序，不能将教师分歧直接当作学生错误，也不排除其他位置或教师自身的问题。

在相同初始化、2000步、价值权重0.1和长度桶256下，只去掉341个纠错分步再训练，实际2000次更新完成、数值健康。保留的46811个训练分步逐元素不变，原验证文件不变，初始验证报告也完全相同。无纠错FP32验证策略loss为1.56053、根准确率28.76%、价值MSE为0.64020、终局等权MSE为0.79769；含纠错分别为1.57670、29.33%、0.83955、1.03750。指标有升有降，单初始化且删样改变桶和抽样序列，尚不能认定纠错稳定有益或有害。

部署落点薄弱已被单独量化：含纠错模型在验证集504处只命中22处（4.37%），训练2348处也仅72处（3.07%）；无纠错对应25/504、110/2348。方向验证命中约96%，不能用总体参数准确率掩盖部署问题。含纠错首次部署9/10在最右列，无纠错改变了列偏好，但低命中仍在。

随后从训练集非纠错部署样本固定抽64处，以同一11.7M结构从头拟合1000步。1000次更新完成、数值有限，BF16与FP32均命中63/64（98.44%），价值标签数0。这说明现有结构能够拟合这些落点，不证明泛化；名义遍历和任务混合也不同。

两模型在新种子2026092591–2595上各10局换边对easy/40：含纠错0胜8负2截断，无纠错1胜9负，均无中断/非法提交；两局截断都达到1200命令上限，不能算平局或失败。20局共用5个独立种子族，9442条命令重放通过。单个胜局不足以证明稳定棋力提升，当前也没有纠错稳定收益证据。下一步应固定数据与总预算，做部署/参数阶段采样分配对照并检查根动作与其他阶段是否退化，再在多个初始化和新种子对战复验。当前仍不升级发行模型或自动提交、推送，原定时检查保持暂停。

## 2026-09-25 结构、数据与训练分配的进一步调查

已完成[固定数据的采样对照](experiments/SAMPLING-2026-09-25.md)，产物`artifacts/training/sampling-20260925/`。两个初始化种子各比较自然采样和每批8/32部署落点重采样，共四组；同47152个训练分步、原验证族、11.7M结构、学习率、价值权重与长度桶。每组2000次实际更新、共8000次，数值健康；检查点、配对初始化、原训练/验证及114个源分片、源码和脚本指纹核对通过，未改变标签或规则。

完整FP32评估：初始化20260922的训练部署命中3.24%→10.69%，验证4.56%→5.95%；20260923对应2.94%→10.65%、4.17%→7.34%。移动两组改善，但根动作分别30.05%→30.51%、30.91%→29.41%；价值MSE分别0.48857→0.66405、0.45960→0.86502，均退化。部署曝光增加约6倍，同时价值监督曝光减少约25%、全体样本覆盖下降，优化耗时增加约36%–44%。训练分配是已确认影响因素，但本方案不是整体更强模型的证据；本轮未跑新对战。

两个初始化和5个验证种子族不足以作统计定论。当前自然组与历史同配置候选的价值指标存在明显差异，原因待查，不能声称位级复现；使用本轮同驱动配对结果。下一步做全量部署专项拟合曲线，再分离数据覆盖与空间结构效率。本轮完成检查已收尾，下一项实验尚未启动。

已完成的独立核对：部署训练/验证各自没有相同编码输入的冲突标签；验证7/504个部署编码也存在于训练中，保留原种子划分并如实报告。仅用训练标签拟合的席位落点频率基线在验证命中33/504（6.55%），高于原模型22/504（4.37%），因此不能把原部署薄弱只解释为缺少新数据。64样本可拟合不证明结构的空间泛化效率；数据覆盖、结构和训练优化可能共同影响，先用当前采样对照分离训练分配的作用。

## 2026-09-25 全量部署专项拟合

已完成[全量部署专项拟合曲线](experiments/DEPLOYMENT-FIT-2026-09-25.md)。固定原2348个训练部署与504个验证部署，原11.7M结构、两个初始化各6000次实际更新，总计12000次；每组7个固定点做FP32评估，不按验证选择停止时刻。14个评估点、8个检查点、参数及优化器状态健康、原分片与源码哈希均核验通过。全部价值遮罩为False，不添加标签或更换原验证族。预检曾因脚本误设全部样本需有多候选而停止，修正后保留唯一的单候选样本及失败记录，未进行失败训练或更改数据。

两个初始化在2000步训练命中28.79%/31.56%，4000步75.04%/75.77%，6000步86.54%/83.90%；验证最终仅36/504（7.14%）、37/504（7.34%），只比席位频率基线多3/4例。验证loss从初始约3.79恶化到16.01/17.65。每组192000次曝光覆盖全部训练例子，至少见过50/52次，不能把剩余误差解释成从未抽到样本。

这是当前结构/数据/训练组合发生过拟合的证据，但仍有约13%–16%训练误差，不能宣称优化完全解决。排除7个与训练相同编码的验证样本后，另外497例仅命中30/31例（6.04%/6.24%）；固定5族分组与席位明细见报告。继续增加专项步数未解决泛化，数据覆盖与结构的空间泛化效率仍未分离。下一步固定数据和训练预算比较候选落点空间上下文结构，再做按族的数据规模曲线。专项模型不能替换完整策略；无新增对战、无发行升级，未创建新自动化，结构对照尚未启动。

## 2026-09-25 部署空间结构对照

已完成[候选空间上下文结构对照](experiments/SPATIAL-ABLATION-2026-09-25.md)。固定原2348个训练部署、504个验证部署，两初始化各比较原结构和按落点距离聚合场上实体的空间结构，四组各6000次实际更新，共24000次。可训练参数均11,695,874，配对初始权重和批次序列哈希完全一致。空间偏置边界、退回全局后的前向/梯度一致性、28个曲线点、8个检查点、数值健康及原数据/源码哈希核对通过；正式加载器实际拒绝全部实验检查点。

固定6000步：初始化20260922训练命中86.46%→89.05%，验证41→66/504（8.13%→13.10%）；20260923训练83.09%→89.65%，验证45→47/504（8.93%→9.33%）。验证loss分别16.00874→14.24430、17.43797→15.18307，仍明显过拟合。两组在相同2000步时空间版本训练命中约56%/61%，高于原结构29%/27%，说明空间聚合改善拟合效率；但后期验证优势减弱，不能据早期最好点宣称稳定泛化提升。

第一组5个验证族均改善，第二组仅1族改善、3族退化、1族持平，净增仅2个命中。排除7个与训练相同编码的样本后，497例验证命中为35→59、39→41，跨初始化的不稳定性仍在。现有证据不足以指定结构为唯一或主要根因，数据量/覆盖与教师质量尚未受控检验。下一步固定原结构，只按完整种子族改变数据规模。没有新增对战或发行升级、没有创建自动化，数据规模对照尚未启动。

## 2026-09-25 按种子族的数据规模对照

已完成[数据规模对照](experiments/DATA-SIZE-2026-09-25.md)。所有组统一排除84个纠错部署分步，只比较相同教师配置的自然部署数据；按预先固定的族ID哈希顺序构造6/12/23族嵌套子集，分别698/1341/2264例。原结构、两初始化、学习率、batch、桶和原504例验证固定；六组各6000次真实更新，共36000次。48个曲线点、18个检查点及两个预定终点的12次FP32重载复评通过；参数、梯度、优化器状态有限，原分片、原始来源和源码哈希匹配，整族隔离与零价值遮罩保持不变。

固定6000步，两初始化的验证命中随6/12/23族分别为30/35/30、25/31/35（分母504），没有跨初始化稳定的单调收益；训练命中84.58%–91.26%，验证NLL17.12–33.62，明显过拟合。预定32次名义曝光时，总验证命中为28/36/44、31/32/39；统一排除最大池重复的7个验证编码后，固定497例命中为24/34/37、30/31/32，第二次初始化6→23族只净增2例。等曝光比较同时改变更新数，且仅一条嵌套族路径，不能把总体上升全部归因于纯数据量。

固定23族从2264步继续到6000步，两初始化训练拟合更好，验证命中却44→30、39→35，验证NLL均明显恶化。数据覆盖可能有帮助，重复训练也会损害泛化；现有证据不能指定结构、数据量或方法为唯一根因。下一步优先固定数据与结构做单项训练正则化对照，再验证完整策略各任务与新种子实战。当前不再直接追加相同来源的长训；没有新增对战、发行升级、提交、推送或自动化，正则化对照尚未启动。

## 2026-09-25 训练协议与搜索路线文献调查

应用户要求完成[论文、实验数据与本地实现调查](research/TRAINING-AND-SEARCH-2026-09-25.md)，本轮未启动训练。当前固定学习率、attention dropout为0，学习率日程与dropout尚未受控验证；已有AdamW权重衰减，不能称为完全没有正则化。此前空间试验也不等于检验了二维相对位置attention或所有Transformer结构。

相关棋类论文表明，输入/价值目标改进可能在Top-1几乎不变时提升实战；Transformer可有效学棋，但已发表强结果的数据量远大于本项目。AlphaZero证明教师预训练不是必要前置条件，Gumbel与Pgx提供小模拟预算路线；新预印本也提醒搜索即时帮助不等于网络学会，几何分布外泛化仍可能很差。文档保留原论文数字、版本、计算条件及其不可直接外推的限制。

修订下一步：先补独立战术评估与搜索正确性契约，测32/64/128模拟的有界搜索增益；固定数据上另做学习率衰减与dropout有限对照，再比较更明确的空间表示。只有搜索确实改善决策，才扩大搜索分布监督与自对弈。保持公开Observation、独立模拟随机数、按实际操作者回传、完整命令和截断价值遮罩边界；当前尚未实现或执行这些新实验。

## 2026-09-25 学习率与dropout固定数据筛查

已完成[固定数据正则化对照](experiments/REGULARIZATION-2026-09-25.md)：2264个自然部署训练样本、原504个验证样本和原结构不变，两初始化各执行恒定/余弦学习率 × 残差dropout 0/0.1，八组各2000步，共16000次实际更新。2000步是运行前确定的有界筛查终点，不与此前6000步混为同预算比较。48个曲线点、16个检查点FP32重载复评、数值健康、配对权重与批次哈希、数据及源码哈希、整族隔离和零价值遮罩均核对通过。

不加dropout时，余弦使两初始化验证NLL5.93599→4.66048、5.51708→4.38618，但命中分别38→32、31→40/504；去重复497例后31→26、26→34，仍不稳定。单独残差dropout使命中38→35、31→30，未见收益；与余弦组合为32/38次命中，也未显示额外改善。余弦降低了相对教师标签的过度置信，四组最终NLL仍高于各自1000步。现有证据不支持“只需加衰减和dropout就能解决问题”，也不能否定Transformer或指定单一根因。

搜索接口核对确认已有公开随机转移与终局边界，但完整命令先验、机会节点和按实际操作者回传仍需实现。特别是回合外巨大化不能只用`toPlay`概括；首个串行探针必须明确能力范围。当前部署专项价值头未训练，不能直接作为搜索叶点评估。

11项现有环境/解码行为测试重新通过。下一步进入可验证短程战术局面的32/64/128次有界搜索增益探针，分开测先验和叶点评估，不再以教师Top-1阈值为前置条件；搜索有效后再采集搜索策略分布并做自对弈学习。本轮未实现MCTS、未新增对战、未升级发行、未提交推送或新增自动化。

## 2026-09-25 短程PUCT搜索探针

已完成[短程搜索实验](experiments/SEARCH-PROBE-2026-09-25.md)：CLI均匀先验PUCT，完整命令为边、独立模拟概率后继，价值按实际操作者最大/最小化。首轮限定经典play/强制反应、两条命令，拒绝暗选和回合外巨大化；终局之外的边界0只是估计，没有训练标签或网络调用。

30个人工局面、4个搜索种子、0/32/64/128模拟，共480条决策。相同两步全宽概率参照下，最优选招分别58/114/120/120（各120次）；32次漏掉5个死亡反应陷阱及1次连续攻击。480条预算/命令审计、30个精确参照重算、30次确定性复跑通过，TypeScript和369项行为测试通过。

追加现有教师对照：easy/40为24/30，hard/800已30/30，且这批简单题上hard更快。新搜索已证明能超过均匀不搜索基线，**尚未证明超过困难教师**；这些相关人工夹具不是独立对战，不称为胜率。下一步在固定独立种子族的自然局面上检验搜索增益与成本，再决定是否采集搜索分布训练学生。本轮没有模型训练、完整比赛、发行升级、提交推送或自动化。

## 2026-09-25 分批提交与自然局面评估

现有成果已按纠错轨迹、分片训练与诊断、随机PUCT探针、实验文档四批提交，分别为`efa8e38`、`286227b`、`17da48a`、`d5a842f`，未推送。随后启动[自然局面搜索评估](experiments/NATURAL-SEARCH-2026-09-25.md)：从未参与训练的2026092591–2026092595十局换边开发轨迹（8终局/2截断/0中断）中，先重放再固定选取20个位置；160次PUCT请求加20次hard/800教师对照，保留全部超限和不支持的位置。这是复用开发种子，不是新的盲测。

全批已于18:23完成，四进程独立复核通过：10局重放与固定选样、全部180条请求、137条返回命令合法性、20次参照重算及20次128模拟确定性复跑均核对一致，源码/数据指纹保持。0/32/64/128模拟分别返回32/29/28/28条命令（各40请求）；其余明确暂停，均涉及尚未支持的回合外巨大化。非零预算85次成功返回的选招全部与各自零模拟基线一致，终局叶为0。

16个能完成根枚举的位置中13个超过128条命令；128模拟成功的28请求中22个只走到第一条命令。均匀探索、首次展开返回零估计和缺乏有效叶端回报使搜索没有区分动作。精确参照11个超限、4个不支持巨大化、4个概率树未完整，仅1个完成且只有一个强制动作；不能将该位置的2/2最优统计称作搜索准确率。此轮没有调用网络，不能据此认定Transformer不适合或模型结构是根因。

串行主实验约98.5分钟；耗时反馈后将尚未开始的复核改为四进程动态分配，正式计算约10.47分钟。两者工作不同，不宣称四倍加速。128模拟仅成功请求的中位数约45.66秒，现有hard/800在20位置均返回命令、中位数约0.80秒；预算含义与样本过滤不同，尚无充分真值比较棋力。

下一步先按局面并行做枚举/转移耗时剖析、小范围叶端估值与展开策略单项对照，补充可区分动作的自然战术评估，再决定是否采集搜索分布；不继续扩大当前零值搜索。训练更新/网络调用/新增标签均为0，没有新增比赛或发行升级；未启动下一轮或新的自动化。详细失败覆盖、种子范围和比较限制见实验报告。

## 2026-09-25 展开成本与叶端估值对照

已完成[成本/叶端对照](experiments/search/LEAF-COST-2026-09-25.md)。沿原固定清单取前四个不同开发种子族各一个位置（3支持、1因巨大化暂停），另保留30人工夹具；两搜索种子、16模拟、相同两命令深度。首轮三方法204请求，四进程计算约82.79秒；看到退化后追加父节点初值对照136请求，约9.87秒，明确属于探索性补充。

原零估值方法在六个支持的自然请求中，99.86%的耗时用于动作枚举。把新叶完整枚举延迟到第二次访问，六请求累计90.42→5.06秒、配对耗时下降15.27–18.85倍，展开102→6；选招、根访问/机会统计和转移数一致。这是三个位置的工程证据，不能外推完整对战速度或称作棋力提升。原默认行为保持，新增选项仅供实验。

手工叶值虽使6/6自然请求出现不同边值，但人工夹具从零估值47/60退化到29/60。已复现高正估值将16次访问锁在首次抽到的结束回合：未访问边默认0，没尝试获胜攻击。改为父节点估计后，一步获胜和连续攻击恢复到各12/12，总计39/60，但随机获胜0/12、死亡反应陷阱3/12；局面优势与短程终局回报不等价，不能把当前手工评分升级为可靠价值头。

独立重放、340条请求核对、204个廉价变体确定性复跑、30个旧默认行为对照及全部数值健康检查通过。第一阶段源码按协议哈希留存快照，上轮自然实验已本地提交`5edc159`，没有推送。下一步保留成本优化，验证机会/反应场景的价值目标与有限rollout，并补独立自然战术参照；不继续放大当前手工叶值配置。新增训练更新/网络调用/训练标签/完整对局均为0，没有发行升级或新自动化。

## 2026-09-25 短续演终局目标对照

已完成[短续演实验](experiments/search/TERMINAL-ROLLOUT-2026-09-25.md)：同样16次外层模拟、两命令深度，增加easy/40选招的一命令叶端续演，只以实际终局作为回报。42位置、两搜索种子、三方法共252请求，四进程正式计算80.31秒；全部252请求另行复跑一致。教师内部计算单列，不能称为等计算预算比较。

人工夹具零估值47/60、手工父节点初值39/60、终局短续演60/60达到短程参照最优；随机获胜与对方死亡反应均恢复12/12。普通自然组仍只有三个位置支持，96次续演全部未知，累计耗时6.82→18.49秒且选招没有改善。教师只提供选招而非价值真值，但仍引入教师知识；尚未证明超过困难教师。

另按预先固定规则取原8个终局游戏的最后决策，三个位置支持、三个巨大化不支持、两个枚举超限；这些是旧开发种子中的终局定向样本，不是新盲测。受支持位置根命令数148/183/370，短续演6请求均只访问第一个根动作，未选立即获胜命令。不过独立完整概率证书确认6/6都保留同一操作者的确定两命令获胜路线，不能记作6次战术失败；下一决策能否实际执行这条路线仍未验证。

独立审计通过10局重放（8终局/2截断/0中断）、30个规则参照、216条合法返回命令及36条明确暂停、136条上轮重复基线、预算/有限性/汇总核验。TypeScript、374项行为测试、格式和结构检查、构建一致性与35项Chrome离线file://验收通过。没有模型更新、网络调用、新训练标签或完整比赛；固定验证族、纠错零价值遮罩和发行模型保持。

下一步检验连续重决策能否兑现已找到的获胜路线，再比较根覆盖/先验分配；有收益后使用新独立种子族验证完整对战，不扩大当前配置的自对弈。当前新增证据涉及搜索目标、稀疏回报与动作预算分配，不能据此否定Transformer。本轮未启动后续长任务或新自动化。

## 先后手差异：作者反馈与待验证假设

2026-09-22，用户转述作者原话：

> AI模拟结果可能跟真实情况也有差异，实际玩下来其实没有想的那么大，因为某些血低、手长的，你先手下下来之后，后手可以针对性的贴脸布置克制他，如果先手是手短、攻高的，后手就可以布置出来风筝他，本质上不是卡牌类的回合制，反正当年实际游玩下来，先后手差距没有那么大，不过后手确实有一定劣势，但是浩劫里后手的劣势绝对会比象棋里后手劣势还要小

这是作者的实战经验反馈，不是规则修改，也不是已测得的胜率。现有40局中先手29胜、后手8胜、3局截断；只计37个终局时先手占78.4%，不能把它当作所有对局的真实先手胜率。17个完整种子对中，9对都是先手获胜、7对都是hard获胜、1对都是medium获胜。当前教师的席位效应值得认真处理，但样本共用种子族与评分逻辑；交换难度不消除共有的战术盲区，同种子也不保证不同决策轨迹下抽牌序列完全镜像。

查阅现有实现后，**教师没有充分兑现后手的针对性部署能力，是有代码依据、尚待实验证实的假设**：

- `evaluation/evaluate.ts`的`placementValue`已有射程、接敌距离、支援、受伤与死亡风险，不能说完全不考虑克制。但普通进攻单位的距离奖励倾向更快接敌和深入射程，并没有单独表达相对某个短手敌人的可持续安全距离；`incoming`主要从对手下一实际行动窗口的现有位置估算攻击，完整追逃仍需搜索补足。
- 困难档部署等普通动作组在根部最多保留4个成功模拟的候选，后续展开每组最多1个，束宽2、主搜索深度配置为7层原子动作而非7个完整回合；总预算可能提前停止。落点先由手工分数排序，若一条针对性部署起初评分低，可能根本没有进入后续推演。增加800到1600节点没有放宽这些上限。
- 对手回应仅检查最终前2个根方案，续局使用easy级窄策略，每步最多3个动作组、各1个候选。它可以捕捉部分直接反击，但不等于完整搜索对手的最佳部署或连续风筝。32局面预算审计中hard/800只有14处实际采用完整回应；这些是旧语料位置，不是本批40局的回应覆盖率。

先手主动施压和后手利用已知部署进行反制，对策略能力的要求可能不同，因此双方使用同一套不完善策略也可能放大席位差异。目前不能确定游戏本身的后手劣势大小，也没有可比证据支持或反驳“比象棋更小”。规则忠实重放和AI能否代表熟练玩家是两项独立验收；当前不因这批结果修改平衡规则，也不把后手败局直接视为劣质训练样本。

下一轮教师质量检查优先加入这两类战术：在合法可达或完整校验的公开局面中，比较贴脸压制脆皮远程、远程对短手的安全部署与后续走位。核对部署行、行动时机、路径与真实随机边界，以同一完整行动窗口的实际交换和可验证战术结果评判，不能仅凭原评分器给作者方案判错。先定位候选是否生成、是否被裁剪、是否被错误估值，再做定向修改和独立局面复验；随后用相同预算的hard对hard及与旧版换边对战检查棋力与席位变化。即使同档自对弈更均衡，也不能单独证明人类实战平衡。训练数据继续按席位报告，补足后手成功反制示范；最终结论需要更强策略与人类实战交叉验证。

## 本轮工程验收

2026-09-25分批提交前统一复核：TypeScript、369项行为测试、Python 9项测试、Ruff与Python格式、全仓Prettier（`--end-of-line auto`保留既有Windows行尾）、目录结构和diff空白检查全部通过。`deploy:check`重新构建823,464字节离线HTML，与根发行文件逐字节一致；Chrome完成35项**离线file://**浏览器验收，无外部网络请求或未捕获错误。这是工程回归验收，不代表模型或PUCT完整对战强度已通过。

此前TypeScript检查、326项行为测试、Python 6项测试、Ruff与格式检查、目录结构检查均通过。构建与deploy:check通过，根HTML和新构建逐字节相同；原游戏34项浏览器验收以**离线file://**通过。该离线结果只验证原游戏发行物；模型浏览器结果为上面的本地HTTP测试。

本轮CLI交付通过TypeScript、332项行为测试、Python 7项测试、Ruff、目录检查和deploy:check内的完整构建，发行HTML仍为807,743字节且逐字节一致。本轮未改页面，未重复浏览器UI验收；不能据此把网络的离线浏览器接入标记完成。新增/修改的TypeScript和文档通过默认Prettier检查；全仓使用相同配置加`--end-of-line auto`检查，避免重写既有Windows CRLF文件。此前默认全仓`format:check`因这些既有行尾报222个文件，记录保留。

教师试采工具通过TypeScript、335项行为测试、Python 7项测试及Ruff；新增检验覆盖混合教师换边、同族隔离、重复配置拒收、中断标签、按教师筛选和真实命令编码。构建与deploy:check仍通过，发行HTML没有变化。采样的原始源码指纹与后续编码/审计工具修补后的指纹分别保存；本轮没有修改引擎和教师规划算法。

提交前再次运行deploy:check，根HTML与新构建逐字节相同；34项浏览器验收重新以离线file://通过，无外部网络请求或未捕获错误。此次补充记录作者对先后手的反馈与验证计划，没有改动游戏规则或AI策略。

## 已有记录

| 日期／提交           | 工作                                       | 证据                                           |
| -------------------- | ------------------------------------------ | ---------------------------------------------- |
| 2026-09-22 / c0d5cd3 | 高速对弈环境、常驻协议、教师样本与Node基准 | [Node报告](TRAINING-NODE-2026-09-22.md)        |
| 2026-09-22 / db2fe7e | Python 3.12/PyTorch骨架与CPU/XPU对照       | [训练性能报告](TRAINING-PYTORCH-2026-09-22.md) |
| 2026-09-22 / f550787 | 真实数据编码、v2小样本训练、浏览器推理基准 | 本文件的真实样本与浏览器实测章节               |
| 2026-09-22 / 19c19fa | 网络分步解码、常驻推理、20局CLI对战验收    | [CLI网络实验](benchmarks/neural-20260922.json) |

本机Core Ultra 5 225H / Arc 130T / 32GiB内存。固定64实体槽、64候选、batch 128的完整优化步：CPU FP32约96样本/秒，XPU BF16约466样本/秒。它不包含真实编码、采样或MCTS。教师旧基准约6–8条命令/秒，采样可能比训练更新更费时；先测2/4个教师进程的实际扩展，再决定长期采样配置。

## 实验记录要求

每次保留规则/编码/模型版本、源码提交或内容指纹、输入数据指纹、训练/验证整局分组、教师难度和工作量预算、设备精度、训练步数及检查点。报告分开列出：

- 数据：完整/截断/中断局数、真实决策数、分步样本数、动作/棋子覆盖、被拒绝或未支持的样本；不得静默丢失。
- 学习：策略损失、价值损失、有效价值标签数、验证指标、实际优化更新数；截断局不充当平局。
- 对局：对手版本、种子集合、先后手、预算、完整胜负与截断率、非法尝试与强制反应是否完成。
- 性能：采样、编码、加载、训练、推理、完整决策分别计时；首次加载/编译与稳态分开。
- 浏览器：实际file://离线模式、CPU/WebGPU后端、输出差异、峰值内存和批量；生产仍只有一个离线HTML。

当前没有已训练的正式游戏模型，也没有模型对作者胜率数据。

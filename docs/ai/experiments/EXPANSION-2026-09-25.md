# 新局面、教师重标与分片训练

2026-09-25同日后续。基于[价值消融结果](VALUE-ABLATION-2026-09-25.md)扩大数据来源。流水线于2026-09-25 11:36:53（UTC+8）正常完成，`status.json`为`complete`，错误日志为空。产物目录`artifacts/training/expansion-20260925/`。价值误差有所下降，策略泛化与对easy实战没有改善证据，候选不升级为发行模型。

## 冻结的实验安排

- 新教师语料：经典模式，种子2026092521–2026092528，每种子hard/800与medium/320换边，共16局；4个进程动态分发，每局100个全局回合或3000条命令上限。两个预算均为work，记录真实终局或截断，不按血量判胜。
- 学生局面：从上一轮纯策略4000步模型的10局实际轨迹选取，种子2026092501–2505。每回合/阶段/操作者/反应标志取首个需要推理的学生决策，再按公开指纹哈希每局最多32个；先选位置再问教师，保留一致标签，不按分歧筛选。
- 教师标签：公开Observation上的hard/800独立冷计划，不继承原缓存或预算历史。命令通过共享动作树、公开预检和固定独立模拟源的一次引擎执行；这不证明穷尽随机结果或优于学生。反事实标签没有实际教师续局，全部价值遮罩为False。
- 固定划分：旧40局轨迹全部保留，原15/5训练/验证种子族不变；新增教师族和纠错源族只进训练。原验证集必须保持10530分步、5450根决策、3359个已知价值标签。旧学生测试种子从本轮开始属于开发训练数据，不再视为未见测试。
- 新对战测试：预留2026092581–2026092585，候选与旧0.1权重1000步模型各10局换边对easy/40；CPU FP32、2线程，100全局回合/1200命令上限，解码256节点/32前向。先前胜负未参与这些种子的选择。

候选从相同初始化种子20260922重新训练11.7M网络，CUDA BF16、batch 32、学习率1e-4、价值权重0.1，2000次训练步，长度桶256。训练后另做CUDA FP32整集评估。相对于旧1000步模型，本轮同时改变数据、分桶和步数，属于扩量探索，不能将变化单独归因于纠错样本；对局比较共用测试种子，不等于新增独立种子。

## 已完成的边界验证

`corrections.jsonl.gz`已完成165个教师标签，其中139个完整命令与学生原命令不同。分歧不等于学生错误，标签一致也不等于教师最优。文件保存原轨迹相对路径、SHA256、命令位置/公开指纹及教师标签，没有逐位置Observation快照。读取时通过共享引擎重放原轨迹并逐条定位；缺尾、重复、来源变化和非法标签均拒绝。

真实纠错文件独立预处理检查得到341个分步、165个根决策，全部价值遮罩为False。检查用临时划分134/31根决策，仅用于验证入口，不用于正式扩量划分。11个小分片完成读取；tiny网络CPU FP32、3次真实更新、长度桶16的冒烟检查通过，参数和梯度有限。这个小模型不是正式候选，不评价其棋力。

正式分片大小512，每个分片保留完整实体/候选，索引`train.pt`/`validation.pt`绑定逐文件SHA256。只读mmap避免一次性按全库最大形状展开，取样时再填充当前批次；预处理仍保留变长样本，操作系统页面缓存仍可能占用较多内存。长度桶先均匀选择样本锚点，再在锚点所属桶抽样，末桶按实际样本数获得概率，不按胜负加权；这还不是按完整命令或整局平衡的采样。

Python 9项测试通过，包括旧/分片数据同一索引（含重复和跨片）等价、原划分固定、分片损坏拒绝、长度桶确定性与既有训练/续训边界。367项TypeScript行为测试、类型检查和目录检查通过。游戏规则、输入编码schema与发行模型未改变。

## 数据与完成核验

8个种子对全部完成，共18102条命令，14局真实终局、2局达到100全局回合上限、0中断。hard为11胜3负，2局截断不计胜负；hard先手8胜，后手3胜3负2截断。这只是本批混合教师语料分布，不是对人类的棋力结论。

| 种子       | 终局 | 截断 | 中断 | 命令数 |
| ---------- | ---: | ---: | ---: | -----: |
| 2026092521 |    2 |    0 |    0 |   1866 |
| 2026092522 |    2 |    0 |    0 |   1202 |
| 2026092523 |    2 |    0 |    0 |   2733 |
| 2026092524 |    1 |    1 |    0 |   2931 |
| 2026092525 |    2 |    0 |    0 |   2538 |
| 2026092526 |    2 |    0 |    0 |   1867 |
| 2026092527 |    1 |    1 |    0 |   2726 |
| 2026092528 |    2 |    0 |    0 |   2239 |

两局截断均为hard后手：2524有2443条命令，2527有2362条命令，没有价值标签。采样墙钟约55.60分钟（4进程动态调度）；不能与不同局面或并发条件的历史速度直接比较。

正式预处理完整重放旧6份轨迹、新8份轨迹和纠错来源。训练集28个种子族、47152个分步、23881个根决策、19098个价值标签，共93个分片；验证集原5族、10530个分步、5450个根决策、3359个价值标签，共21个分片。训练包含46局教师轨迹和10局学生来源的局部教师重标；后者不是新增教师完整对局。训练价值标签来自42个终局，胜/负为14369/4729；验证来自8个终局，胜/负仍为1035/2324。

完成审计逐批比较了新旧验证集的**全部张量，逐元素完全相同**，记录顺序、种子族也完全相同。固定验证种子为2026092410、2417、2416、2401、2409。正式训练集中的341个纠错分步、165个根决策再次确认价值遮罩全为False；并非仅检查早先冒烟数据。训练、验证与新测试族之间无交集，逐分片SHA256验证通过。证据为`completion-audit.log`。

## 训练与FP32复核

CUDA BF16实际完成2000/2000次更新，无跳步，参数与梯度均为有限值。优化循环91.88秒，不含全量前后评估、加载与保存；分桶、样本分布和步数同时变化，不作为受控加速结论。名义抽样量为2000×32/47152≈1.36次有放回分步遍历，不保证每个样本都被访问。

下面统一为CUDA FP32整集评估，旧模型读取上一轮`value-01-fp32.json`，候选读取本轮`candidate-fp32.json`。

| 模型                         | 验证策略loss | 验证多候选根准确率 | 验证多候选步骤准确率 | 验证价值MSE |
| ---------------------------- | -----------: | -----------------: | -------------------: | ----------: |
| 原40局、权重0.1、1000步      |      1.57404 |             29.44% |               43.84% |     1.00023 |
| 扩量、权重0.1、2000步、桶256 |      1.57670 |             29.33% |               43.57% |     0.83955 |

候选训练策略loss为1.56396，多候选根准确率31.30%，训练价值MSE为0.05666。验证价值MSE比旧模型约低16.1%，也低于零常数1.0、仅用训练标签拟合的全局常数1.64219和席位常数1.27650。但8个终局等权MSE仍为1.03750，超过零常数；先手MSE为0.69579，仍差于训练席位常数0.56999，后手MSE为0.92454。价值改善依赖汇总口径，且只有5个验证种子族，尚不足以确认为可靠搜索叶点评估器。策略指标没有改善。

## 新种子换边对战

两组均在2026092581–2026092585的每个种子上交换网络席位，与easy/40对战。共享5个独立种子族，共20局运行，不能算作20个独立种子。两组每个种子的先后手均失败。

| 模型          | 网络胜 | 网络负 | 终局 | 截断 | 中断 | 非法提交 | 总命令 |
| ------------- | -----: | -----: | ---: | ---: | ---: | -------: | -----: |
| 扩量候选      |      0 |     10 |   10 |    0 |    0 |        0 |   3788 |
| 旧0.1权重模型 |      0 |     10 |   10 |    0 |    0 |        0 |   2851 |

汇总后再次通过共享引擎重放两组6639条命令及终态指纹，结果一致，见`completion-replay.log`。对战报告均无异常、取消或失败。命令更多不证明棋力更强。这是限定种子、贪心分步解码的开发评测，不是对作者胜率，也没有验证MCTS或浏览器模型部署。

## 结论与下一步

本轮完成扩量、纠错标签接入、分片训练和独立种子比较，仍没有策略泛化或实战改善证据；保持现有发行AI。不要把价值MSE下降单独归因于165个纠错根决策：数据、分桶和训练步数同时改变，缺少单因素对照，且纠错仅占训练根决策约0.69%。

下一步优先对已保存败局做决策诊断，区分候选缺失、搜索裁剪、教师估值和学生排序问题；先核对贴脸压制、安全部署与后续走位，以及合成/路径专项正例。随后固定步数、分桶和初始化，做有/无纠错的配对对照，并保留新的未见种子。不要仅凭当前结果继续加步数或接入MCTS。

流水线、原始轨迹、历史指纹和检查点均保留，没有自动提交、推送或模型发布。自动化“浩劫训练扩充后续检查”（ID `automation`）在本轮汇总后暂停。日志为各阶段同名日志和`pipeline.stderr.log`；不要重复运行`continue.ps1`覆盖本轮产物。

关键指纹：候选检查点`b485e87c62230fa47f65e3ca0ef4ac3ef2b62e7296816d7caf3680c0591f5fc8`；旧模型`be4567b74355e76221ef69868cac3bd57538b5e1243f331f01f483da25e5e10a`；采样源`bacd664f302c597e83fffd489291653a0dc3cd19f7c8d0faf6ebafeaa598a11d`；编码源`0c915b2af03a39a52a221d52e7aebe43d3eae0ab99464fd3d86ce90a93253023`；Python训练源`244db9c5244327153077ca5a313858c8d01147cfe98fec273b5a057e14cd0958`。完整输入/分片指纹见`encoded/manifest.json`及`.pt`索引，评估数据和模型指纹见`candidate-fp32.json`。

## 复现入口

命令说明见[训练README](../../../training/README.md#学生局面教师重标)。本轮完整后续参数保存于产物目录的`continue.ps1`；核心采样命令如下，复现须换新输出目录：

```powershell
node scripts/training/dispatch/self-play.mjs --games 16 --seed 2026092521 --workers 4 --rules classic --difficulty hard --nodes 800 --opponent medium --opponent-nodes 320 --plies 100 --commands 3000 --output-dir artifacts/training/expansion-20260925/teacher
```
